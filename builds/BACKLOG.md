# DC20 Builder & Companion, Backlog

Single home for **app / tooling** work (the builder, the Companion, the engine). Living doc, updated as we triage. Started 2026-07-16.

**What lives elsewhere:** game-side rulings and player confirmations (attribute destinations, maneuver splits, rules calls) stay in `09_cogm_agenda_GM.md`, which is the canonical tracker for those. This file is only bugs / features / chores for the software. Where a software item genuinely waits on a player answer, it is marked Blocked with the person named.

**Legend.**

- Type: `bug` / `feature` / `chore`
- Area: `engine` (build_engine.py), `catalog` (builds/catalog), `data` (a ledger yaml), `builder` (builder_build.py, incl. the sheet), `companion` (companion-src), `repo`
- Priority: `P1` soon / high value or unblocks other work · `P2` should do · `P3` nice to have. **Provisional, a proper prioritisation pass is still pending.**
- Status: `ready` / `blocked` / `needs-clarification` / `parked` / `done`
- Convention: no em-dashes anywhere.

---

## At a glance

Two tables: open work first, completed work below. An item moves from To Do to Done when its status becomes `done`/`DONE`.

### To Do

| ID | Title | Type | Area | Pri | Status |
|----|-------|------|------|-----|--------|
| FR-12 | Add full DC20 class + ancestry data coverage | feature | engine+catalog+builder | P2 | PLANNED (2026-07-19: full epic GO + refactor-first, see `FR12_PLAN.md`; = Phase 3-4) |
| FR-13 | Live spell & maneuver legality (school/type filtering) | feature | engine+builder | P3 | PLANNED (maneuver-type half DONE 2026-07-19; spell-source half = FR-13a; full legality = `FR12_PLAN.md` Phase 2) |
| FR-13a | Constrained spell-grant sourcing (per-feature source/school + Sorcerous Origin) | feature | builder+catalog | P2 | SLICE 2 BUILT + VERIFIED, AWAITING PUSH 2026-07-19 (Command childed under the Fiendish Magic ancestry trait + Sorcerous Origin explicit node + source-filtered flat path picks; all 3 "off-list" tags cleared). SLICE 1 PUSHED `34ea490`. FR-13a COMPLETE on push. See notes below |
| FR-33 | Rules > Ancestries: split the one big page / add dividers | feature | companion | P3 | ready |
| FR-34 | Rules > Character Creation > Class Talents: merge the many tiny pages | feature | companion | P3 | ready |
| FR-4 | Rename a char / file (display-name slice done `7676679`; handle/file half remains) | feature | builder+data | P3 | needs-clarification |
| FR-32 | Rules > Spells: drop PDF-scan subcategory + split the giant spells page | feature | companion | P2 | needs-clarification |
| FR-35 | Rules > Tables: move class tables into class pages; armour examples with armour | feature | companion | P3 | needs-clarification |
| FR-11 | Gear catalog / picker (gear Tier B) | feature | engine+catalog+builder | P3 | parked |
| FR-26 | Stackable conditions (bleed/stunned) as counts not toggles | feature | companion | P3 | parked |

### Done

| ID | Title | Type | Area | Pri | Status |
|----|-------|------|------|-----|--------|
| BUG-1 | Xanwyn Elven fluency wrong (Fluent should be Limited) | bug | data | P2 | done (2026-07-16) |
| BUG-2 | Subclass/feature-granted languages not modelled as free (Deep Speech) | bug | engine+catalog+data | P1 | done (2026-07-16) |
| BUG-3 | Point-budget messaging: legal-spare vs over-spent, symmetric language verdict | bug | engine | P2 | done (2026-07-16) |
| BUG-4 | Character sheet overlay not mobile-responsive (iPhone) | bug | builder | P1 | done (2026-07-16, real-phone verified by Phil) |
| BUG-5 | L3 attribute picker options show lower-case not capitalised | bug | builder | P3 | done (2026-07-16) |
| BUG-6 | Runt armour mis-modelled + staff Guard / Pact Armor AD+MDR / DR unpopulated | bug | engine+data | P1 | done (2026-07-16) |
| BUG-7 | Runt AD reads 12, RAW says 14 after the armour fix | bug | data | P2 | done (2026-07-16, confirmed with Phil) |
| BUG-8 | Xanwyn missing History (Novice) trade | bug | data | P2 | done (2026-07-16) |
| BUG-9 | Character-sheet button not pinned top-right (wraps below-left) | bug | builder | P3 | done (2026-07-16) |
| BUG-10 | Pact-boon / discipline picker labels show the raw grants dict | bug | builder | P3 | done (2026-07-16) |
| BUG-11 | Chained point conversion (SP->TP->LP) mis-read as over-spent | bug | engine | P2 | done (2026-07-18, found by CH-1) |
| BUG-12 | Runt's granted Psychic spell renders as "Spell_Tagged" + missing from the char-sheet spell list | bug | builder | P1 | DONE (2026-07-18; sheet() now pulls granted_spells via the spell_tagged slot so Tendrils from Beyond lists; slot label maps to "spell"; PUSHED + LIVE-VERIFIED, head 7018b69) |
| BUG-13 | Dice roller drop-N highlights only the first kept die | bug | companion | P3 | DONE (2026-07-18; diceRow/dropLowest mark every kept die + strike the dropped, both drop-N and d20 ADV/DisADV; PUSHED + LIVE-VERIFIED, head 7018b69) |
| BUG-15 | Condition pills on the char sheet lack a pointer cursor on hover | bug | companion | P3 | DONE (2026-07-18; cursor:pointer on .pill, used only by condition pills; PUSHED + LIVE-VERIFIED, head 7018b69) |
| BUG-14 | Rules search results not ordered by rulebook section | bug | companion | P2 | DONE (2026-07-18, Wave B pass 1; top-30 by score then rendered in document order, _results kept in score order for the searchFor fallback; PUSHED + CHROME-VERIFIED) |
| FR-1 | Sort the builder character picker alphabetically | feature | builder | P3 | done (2026-07-16) |
| FR-2 | Finish DR (PDR/EDR/MDR) for the rest of the party + rules references | feature | engine | P2 | done (2026-07-16) |
| FR-3 | Level-up plans for all PCs (like Tan) | feature | builder+data | P2 | DONE - Slice 1 + Slice 2 both pushed + live-verified 2026-07-17 (origin `6c02db2`) - planned levels carry their own enforced skill picks (Hybrid, FR-8 backbone). Cap-raise follow-up split to FR-17 |
| FR-5 | Unsaved-changes guard when switching character | feature | builder | P2 | done (2026-07-16) |
| FR-6 | Show rules text for a chosen trait/ability (link or hover) | feature | builder | P2 | DONE - pushed + live-verified 2026-07-17 (baked rules corpus + linkify + rule panel) |
| FR-7 | Refilter dropdowns to hide already-chosen options | feature | builder | P2 | done (2026-07-16, spell/maneuver/talent/spell_school; ancestry/pact_boon/discipline later) |
| FR-8 | Choice-bearing talents/subclasses need pickers/slots (epic) | feature | engine+catalog+builder | P1 | DONE - all 5 slices pushed + live-verified 2026-07-17; FR-8 EPIC COMPLETE |
| FR-10 | Echo each level's grants into the level's section header (all levels, all PCs) | feature | builder | P3 | done (2026-07-17; generalised from Tanrielle-L5-only to all levels/chars 2026-07-17) |
| FR-14 | Recent Files list in the picker + auto-add on deeplink | feature | builder | P2 | done (2026-07-16, incl. Level A) |
| FR-15 | Trades on the character sheet show a numeric mastery bonus | feature | builder | P3 | done (2026-07-16) |
| FR-16 | Show DR (PDR/MDR/EDR) on the Companion character page | feature | companion | P3 | done (Part A 2026-07-16; Part B toggle-aware 2026-07-17) |
| FR-17 | Plan skill/trade picks can raise a Mastery Limit (cap+) above the level cap + trades in plans | feature | builder | P3 | DONE (2026-07-18; cap+ toggle for skill AND trade plan picks; trades now planned like skills; PUSHED + LIVE-VERIFIED, origin `33a9119`) |
| FR-18 | Replace the "Rung 3 - Step 5" top-left pill (-> "v0.10.5") | feature | builder | P3 | DONE (2026-07-18; badge now "v0.10.5", text-transform:none to keep the casing; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-19 | Refresh the "Decisions: Point-Buy..." subtitle for current accuracy | feature | builder | P3 | DONE (2026-07-18; three cryptic wlabel pills replaced with one "every level-up choice"; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-22 | Remove the "sources: N fetched, N baked" footer note | feature | builder | P3 | DONE (2026-07-18; #srcinfo element + its population removed; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-27 | Line dividers in Maneuvers (and any other long list) | feature | companion | P3 | DONE (2026-07-18; rules-doc Maneuver entries get the spelldiv divider, 29 entries, keyed on the Cost line; other long lists left for review; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-28 | Dice roller reset-to-defaults button | feature | companion | P3 | DONE (2026-07-18; Reset button clears Count/Drop/Mod/ADV, leaves history; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-30 | Char-sheet Skills & Trades section collapsible | feature | companion | P3 | DONE (2026-07-18; Skills & Trades is now a <details> like the accordions; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-31 | Default collapse state for Maneuvers & Trades (-> collapse all, consistent) | feature | companion | P3 | DONE (2026-07-18; all <details open> flipped to closed in template + TAN_ACCORDIONS in build.py; Skills & Trades collapsed too; PUSHED + LIVE-VERIFIED, head 7018b69) |
| FR-24 | AP tracker: "AP avail:" label + left-to-right depletion | feature | companion | P2 | DONE (2026-07-18, Wave B pass 1; label "AP avail: N" + pips deplete leftmost-first; PUSHED + CHROME-VERIFIED) |
| FR-29 | Capture Android hardware back button in Search view | feature | companion | P2 | DONE (2026-07-18, Wave B pass 1; pushState on openDoc + popstate closes the rule doc instead of leaving the app; PUSHED + CHROME-VERIFIED) |
| FR-20 | Reorder a level's decision pickers to match chargen flow | feature | builder | P2 | DONE (2026-07-18, Wave B pass 2; render-order reorder in _decisions to attrs->class->ancestry->resources, grant-children glued to parent; AWAITING PUSH) |
| FR-9 | Expand ancestry traits into slots (like maneuvers) | feature | builder | P2 | DONE (2026-07-18, Wave B pass 2, Option A polish; per-trait pickers + add/remove + auto-cost + budget already existed; added live "Ancestry points: N of M spent" readout + auto ready-slot; no engine/ledger re-plumb; AWAITING PUSH) |
| BUG-16 | Maneuver/spell x-delete created a silent unrecoverable slot shortfall | bug | builder | P2 | DONE + auto-heal COMPLETE (2026-07-18 edit-only safety, origin `a1096e9`; 2026-07-19 the deferred auto-heal half shipped with the grants-only unification - missing maneuver/spell slots now self-heal via an FR-9-style ready-slot; see GRANTS-ONLY detail below; BUILT + VERIFIED, AWAITING PUSH) |
| FR-36 | Colour-coded left accent on decision rows by category (scan long lists) | feature | builder | P3 | DONE (2026-07-18; per-row category colour keyed to FR20_CAT rank - attributes blue #185FA5, class coral #C2410C, ancestry teal #1D9E75, resources amber #BA7517; class is a warm coral so it never blurs against the adjacent blue attributes accent (Darryl live-verify); REPLACES the old amber .newlvl border, which is dropped entirely - the builder-touched ledger notes were dev bookkeeping and cluttered every picker, so editable pickers no longer surface notes; harness section 24; BUILT + VERIFIED, AWAITING PUSH) |
| FR-21 | Category sub-headers for long level sections (Runt L1) | feature | builder | P3 | DONE (2026-07-18; light category sub-headers inside a level with >=5 rows, no nested collapsibles - Darryl's call; short level-ups render flat; ties to FR-36 colours; harness section 25; BUILT + VERIFIED, AWAITING PUSH) |
| FR-23 | Stamina Regen display (own class + Martial Expansion / Martial-path rules), builder + companion | feature | builder+companion+catalog | P2 | DONE (2026-07-18; catalog-driven trigger(s) on the builder sheet + Companion, shared engine helper; new `builds/catalog/stamina_regen.yaml`; Spellblade = errata wording; Runt = two triggers (Spellcaster + Monk, 1/round); Scaletrix = none (SP 0); catalog_verify section 4 + builder_verify section 27; PUSHED + CHROME-VERIFIED, origin `3dd3cb9`) |
| FR-25 | Damage Calculator section (epic) | feature | companion+catalog+engine | P2 | DONE v1 (2026-07-19; single-hit total card between Roll and Skills & Trades: base/hit-grade/crit/per-char add-ons/running total + pull-from-last-roll; catalog-driven `builds/catalog/damage_addons.yaml` + shared `build_engine.damage_addons()`; catalog_verify section 5; AoE/DR/ruling-dependent deferred to v2; PUSHED + CHROME-VERIFIED, origin `44e6e9c`. UI-polish + data-correction follow-ups PUSHED too (polish `b17793f`, corrections `a349201`): same-line notes, wider Pull button, Rage defaults from the sheet toggle, calc preserved across re-renders; Smite split into +1/SP Bound dmg + a one-shot free-enhancement toggle, Spellstrike added for both Spellblades, Erupting dropped from Bonan, gen_damage cap 2=SSL, Min base 3 / Runt base 2) |
| FR-12.0 | Foundation: data-drive class progression (invert CLASS_TABLES out of build_engine.py) | chore | engine+catalog | P2 | DONE (2026-07-19; = `FR12_PLAN.md` Phase 0. Option B chosen: NEW authored `builds/catalog/class_spines.yaml` is the single source of truth for per-level class deltas; `build_engine.load_class_tables()` reads it (cached, candidate-path resolver for CLI/Companion/Pyodide-FS), `replay(ledger, level, class_tables=None)` takes it as passed-in data (stamina_regen/damage_addons pattern); hardcoded `CLASS_TABLES` dict RETIRED; `catalog_build.py` reads the data file instead of importing the dict; `catalog_verify.py` checks spines vs the data; `builder_build.py` bakes `class_spines.yaml` into the Pyodide FS. Provably safe: loaded data == old dict (asserted), the 5 generated class YAMLs changed provenance lines only (spine numbers byte-identical), PARTY_DERIVED byte-identical (sha match), catalog_verify 90/90, builder_verify PASS, CLI + in-browser engine unchanged. Smoke test: a new class added as data-only resolves with ZERO engine edits = the door-open payoff. Also fixed the pre-existing catalog-header em-dash while regenerating. PUSHED + CHROME-VERIFIED, origin `2b80f98`) |
| CH-1 | Re-audit all six ledgers' languages/skills/trades vs paper sheets | chore | data | P2 | DONE (2026-07-18; 5 of 6 clean, minimus filled from sheet; surfaced BUG-11) |
| CH-2 | Standardise Elven / Elvish / Elvan spelling | chore | data+catalog | P3 | DONE (2026-07-18; language = "Elvish" per RAW, adjective = "Elven"; "Elvan" gone) |
| CH-3 | Drop the vestigial tracked root index.html | chore | repo | P3 | DONE (2026-07-18; runs as `git rm --cached index.html` in Darryl's clone with this thread's commit) |

---

## Prioritisation (locked 2026-07-16)

Worked top to bottom. Wave 1 is designed to verify in a single regression cycle.

- **Wave 1, correctness + cheap wins:** BUG-1, BUG-2, BUG-6 (and log BUG-7), BUG-5, plus applying Runt's now-known ancestry itemisation to the ledger. Optionally CH-1 alongside (it validates these data fixes).
- **Wave 2, high-impact UX:** BUG-4, FR-14 (with Level A), FR-5, FR-1, FR-7, BUG-3.
- **Wave 3, bigger features + the new quick wins:** FR-3, FR-6, FR-10, FR-2. Fold in the cheap 2026-07-16 grooming items alongside, since they verify in the same cycle: **BUG-8** (Xanwyn History, data - do with CH-1), **BUG-9** (sheet button top-right), **BUG-10** (picker label grants-dict), **FR-15** (trade numbers on the sheet), and **FR-16** (Companion DR line - Part A pairs directly with FR-2; Part B the toggle-aware buffed DR can follow).
- **Wave 4, epics (interdependent):** FR-8 (now also covers the L1 Pact Weapon picker), then FR-12 (needs FR-7 + picker search), with FR-13 alongside FR-12.
- **Low / when convenient:** CH-2, CH-3, FR-4 (needs scoping), FR-11 (parked).

**Shipped 2026-07-16 (pushed, head `e26963b` then `caca1d0`):** all of Wave 1 (BUG-1, BUG-2, BUG-5, BUG-6 + Runt L1 ancestry itemisation) plus BUG-4 (mobile sheet); BUG-7 now surfaces as a red MISMATCH by design (pending Phil). Baseline is now **89/90** (the 1 delta is BUG-7). Stray `runt_old.yaml` removed from the repo; `BACKLOG.md` added. Companion `template.html` sknotes aligned to the ledger fixes (Xanwyn Elven limited; Runt Deep Speech Fluent Eldritch grant).

**Wave 2 remainder SHIPPED + PUSHED (head `253c30e`) + LIVE-VERIFIED 2026-07-16:** FR-14 (Recent Files in the picker, per-device localStorage, deeplink `?char=` auto-adds; plus Level A: the baked party is no longer listed in the default dropdown but still resolves by deeplink), FR-1 (new-from-scratch list sorted alphabetically; the party list is delisted by Level A so the alpha-sort applies there and recents stay recency-ordered by design), FR-5 (unsaved-changes confirm-guard when switching character in the dropdown; Cancel reverts the selection), FR-7 (pickers hide options already chosen elsewhere for spell / maneuver / talent / spell_school; ancestry_trait / pact_boon / discipline left to a later pass because of their budget / choice-count machinery and existing harness expectations), BUG-3 (symmetric, clearer budget verdicts: balanced / "N SPARE (legal)" / OVER-SPENT on all three of skills/trades/languages; the language line is no longer asymmetric, and "UNDER-SPENT" is retired so a legal lumpy-conversion spare no longer reads as a fault). Harness section (12) added (238 OK; catalog_verify still 89/90). Files: `tools/build_engine.py`, `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`, and this `BACKLOG.md`. Remaining in the plan: Wave 3 (FR-3, FR-6, FR-10, FR-2), then the Wave 4 epics.

**Wave 3 quick-wins SHIPPED + PUSHED 2026-07-16:** BUG-8 (Xanwyn History (Novice) trade added to `xanwyn.yaml`; spent TP 7 -> 8 = balanced, his "1 SPARE" advisory clears; the `builder_verify` section (12) "legal SPARE" example moved off Xanwyn to minimus's 6 spare skill points), BUG-9 (builder header is now a flex row, `.apphead`, with the Character-sheet button pinned top-right via `margin-left:auto`), BUG-10 (a new `_fmt_grants()` helper formats the pact-boon / discipline picker labels, e.g. "Pact Weapon (+2 maneuvers)", "Magus (+1 MP, +1 spell)" - the raw grants dict is gone), FR-15 (the character sheet shows trades with a numeric mastery bonus only, e.g. "Arcana +4" via `MB[tier]`, with a baked note that the attribute is added in play). Verified: catalog_verify 90/90, builder_verify PASS all sections incl. new BUG-9/BUG-10/FR-15 checks + node --check. Files: `builds/xanwyn.yaml`, `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`, and this `BACKLOG.md`. Remaining in Wave 3: FR-3, FR-6, FR-10, FR-2 (+ FR-16).

**FR-2 + FR-16A SHIPPED + PUSHED + LIVE 2026-07-16:** FR-2 - worked out DR for the rest of the party: only Runt has any (PDR Half + MDR Half); the other five wear Light armour or none, so no PDR/EDR/MDR, and the engine already derives empty DR for them (machine-verified in `builder_verify` section (10): runt PDR/MDR Half, all others empty). Xanwyn's "Fire Resist 1" trinket is a type-specific Fire Resistance, NOT general Elemental DR (EDR reduces all of Cold/Corrosion/Fire/Lightning/Poison), so it is correctly not modelled as DR. Added rules references to Runt's DR entries in `runt.yaml`: PDR Half = Heavy Armour PDR (tables.md l.219, char-creation.md l.142); MDR Half = Pact Armor "Mystical Armor: +1 AD and MDR" (classes.md l.3271); DR reduces normal Hits only, not Heavy/Crit (bestiary.md l.318-330). FR-16A - the Companion now shows a DR line: `companion-src/build.py` bakes the engine-derived `dr` into `party_derived[k]`, and `template.html` merges it over `CHARS[k].dr` and renders "DR: PDR Half · MDR Half" under the `defDetail` line via a new compact `fmtDR()` (PDR/EDR/MDR order, "half" -> "Half"), omitting the line entirely when a character has no DR (so only Runt shows it for now). **Part B (toggle-aware buffed DR, e.g. Bonan's Rage lighting up PDR/EDR Half while active) is still to do** - it needs toggles to carry a DR payload plus a `curDR()` that merges base with active toggles take-the-stronger. Verified: catalog_verify 90/90, builder_verify PASS (incl. the FR-16A source-furniture checks), Companion build.py runs clean and bakes runt's DR, companion inline JS passes node --check. Files: `builds/runt.yaml`, regenerated `builds/builder.html`, `tools/builder_verify.py`, `companion-src/build.py`, `companion-src/template.html`.

**FR-10 SHIPPED + PUSHED 2026-07-17:** the sidebar next-level preview (the "+2 HP, +1 SP, ... · Class Feature" strip by the level-up button) is now echoed into the collapsible section header of the level it describes. It fires on the planned cur+1 group, so a collapsed plan shows what that level grants without expanding it (e.g. Tanrielle's "Level 5 (plan)" header reads "grants: +2 HP, +1 SP, +1 MP, +1 spell, +1 attribute pt, +2 skill pt, +1 trade pt · Class Feature"). New `.lvlprev` muted style + a summary-injection in the decisions render loop (`builder_build.py`). Verified: catalog_verify 90/90, builder_verify PASS (exit 0) incl. new FR-10 checks + node --check. Files: `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`.


**FR-10 GENERALISED to all levels + all characters (PUSHED origin head `394a8af` + live-verified) 2026-07-17.** The original FR-10 echoed grants into ONE header only - the single planned `cur+1` group (JS gate `s.next && s.next.level===lvl`), and Tanrielle was the only PC with a planned level (L5), so the grant line showed for her L5 alone. Now every rendered level of every character shows what it grants. Change: in `API_PY` the per-level formatting is factored out of `next_level_info` into a shared `_level_grant(lvl)` helper, and `state()` gains a `level_grants` map covering every rendered level (1..current + planned, L1 included by Darryl's call - it reads the chargen starting kit e.g. "+8 HP, +6 MP, +4 spell"); in the `TEMPLATE` JS render loop the `lvlPrev` injection is repointed from the `s.next` gate to `s.level_grants[lvl]` (same `.lvlprev` style, same "grants: numeric &middot; features" markup, same feature filter dropping the generic "Class Features"). The sidebar Add-level button still uses `s.next` (`next_level_info` intact). Tanrielle's L5 header string is unchanged (regression-locked). Verified in a fresh clone: catalog_verify 90/90 zero deltas (no catalog change), builder_verify PASS all sections incl. the rewritten FR-10 checks (level_grants covers every rendered level for all six chars incl L1; the old cur+1 gate is gone; Tanrielle L5 unchanged; runt/no-plan now shows all levels; the next strip still drives the button) + inline JS node --check + `api blob == builder_build.API_PY`. builder.html regenerated (293,073 bytes). Files: `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`, and this `BACKLOG.md`.

**FR-16 Part B (toggle-aware buffed DR) SHIPPED, PUSHED & LIVE-VERIFIED 2026-07-17 (Companion-only).** The Companion DR line now reacts to active toggles the way PD already does. Bonan's Rage toggle gains an optional DR payload `dr:{PDR:['half'],EDR:['half']}` (half Phys/Elem while raging), and a new `curDR()` (mirroring `curPD()`) merges base engine DR with any active-toggle DR **take-the-stronger, not additive** (`'half'` beats a flat numeric via a `drRank` helper; two numerics take the max; half+half stays half), returning the same list shape `fmtDR` already consumes. `curDR()` is a passthrough of `c.dr` when no DR toggle is active, so FR-16A behaviour is byte-identical at rest and for Runt (base gear DR, no DR toggle). The DR line reads `fmtDR(curDR())` instead of `fmtDR(c.dr)`, so Bonan shows "DR: PDR Half · EDR Half" while raging and it clears when Rage drops (the PD label already flips to "PD (raging)"). Checked the other five characters: none carry a DR-bearing toggle (Kitsune Wild Form sets flat PD/AD; Fiery Form is a form flag with no stated reduction), so Rage is the only real case; the take-the-stronger merge only ever engages defensively (no canon char has both base DR and a DR toggle). **Companion-only** - no `build.py`, `build_engine.py`, catalog or ledger change (toggle DR is hand-authored like `pd:-5`; base `c.dr` stays engine-baked), so `builder.html` is untouched and catalog_verify stays 90/90 by construction. Verified in a fresh clone at head `394a8af`: catalog_verify **90/90 zero deltas**; builder_verify PASS all sections incl. two NEW FR-16B checks (Rage carries the DR payload; `curDR()` exists and the line is `fmtDR(curDR())`); a standalone node harness confirms the runtime merge (Bonan raging -> "PDR Half · EDR Half"; not raging -> ""; Runt unchanged; base-2+toggle-half -> Half; base-3+toggle-2 -> 3; half+half -> Half); the rebuilt Companion's inline JS passes `node --check` and the DR canaries decode in the built HTML. Files: `companion-src/template.html`, `tools/builder_verify.py`.
**FR-6 (rule text on a chosen option) SHIPPED, PUSHED & LIVE-VERIFIED 2026-07-17.** Chosen options in the builder's Decisions list now link to their DC20 rule. Approach (Darryl's calls): bake the rules corpus into the builder and show the rule in an inline click panel, covering all option types. **Single source of truth:** the corpus-building logic (section-split / PDF-reflow / merge) was factored out of `companion-src/build.py` into a new shared module `tools/rules_corpus.py`, now imported by BOTH the Companion build and the builder build, so the two apps bake a byte-identical corpus and cannot drift. The refactor leaves the Companion output byte-identical (proven: same-corpus sha, and a full pristine-vs-refactored build diff = 0 lines beyond the build stamp). In the builder: `builder_build.py` bakes `RULES_DATA` via a new `__RULES_DATA__` placeholder; the Companion's `linkifyTerms` + term-recognition (`_linkable`/`_clean`/`RULE_CORPUS`/`CONDS_SET`/`DEFINED`) + section-resolver (`_home`/`_condTarget`) are ported verbatim; a new lightweight rule panel (`openRulePanel`/`closeRulePanel`, `#rulePanel`/`#ruleScrim`, dismiss via close/scrim/Esc) renders the matched section's HTML through `linkifyTerms` so terms inside a rule are themselves clickable (in-doc cross-links). The Decisions render loop appends a compact `ruleTag()` link after each chosen option (picker rows use `t.current`, since a `<select>` can't hold markup; fixed-text rows use `t.pick`); unknown/`(undecided)` names render plain. **No engine/catalog/ledger change** (API_PY untouched, so section-1 `api blob == API_PY` still holds; catalog_verify stays 90/90 by construction). Size note: `builder.html` grows ~262 KB -> ~2.4 MB from the baked rules corpus (~2.1 MB, the same corpus the Companion already ships) - invisible next to the multi-MB Pyodide load, per the FR-12 analysis. **Deploy note:** `builder_build.py` now imports `markdown` at build time; the deploy Action already `pip install markdown pyyaml` before both builds, so CI is covered. Verified in a fresh clone: catalog_verify **90/90 zero deltas**; builder_verify PASS all sections incl. NEW section **(17)** (corpus baked + shaped {f,t,h,x}; baked corpus == `rules_corpus.build_rules_data(REPO)` - no drift; `linkifyTerms`/`_linkable`/`ruleTag`/`openRulePanel`/`closeRulePanel` present; panel + scrim + term sets present; both render branches route through `ruleTag`; panel renders through `linkifyTerms`; a node harness on the REAL baked corpus confirms a known condition (Prone) and term (Pact Weapon) are linkable + resolve, `ruleTag` builds a link for a known term and is empty for junk/`(undecided)`, cleans a composite to its base name, and `linkifyTerms` wraps a known bold term but leaves an unknown one plain); inline JS `node --check` OK; `builder.html` regenerated. Files: NEW `tools/rules_corpus.py`, `companion-src/build.py` (import from the shared module), `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`. Live-verify via Chrome after push: open the builder `?char=runt` (or any char) and click a "rule" link on a chosen option (e.g. Pact Weapon, or a condition) - a panel should slide in with that rule's text, close on the X / backdrop / Esc, and terms inside the panel should themselves be clickable.

**Demo context (2026-07-16).** Next session is tomorrow night. Nothing here is required for the game, it runs fine as-is: zero must-do, the session is not blocked whatever we do. Everything working by then is an "above and beyond" demo bonus, and players (Phil especially, on Runt) will likely use the builder live on their phones if enough works. So for the demo the highest-leverage subset is the mobile sheet (BUG-4) and Runt's correctness (BUG-2, BUG-6, the ancestry itemisation), since those are what the players would actually touch; FR-14 Level A makes the picker demo cleaner. Anything meant to be live for the demo must be synced + committed + pushed by Darryl (or shown from a local build).

**Intake 2026-07-18.** BUG-12..15 and FR-18..35 captured from Darryl's list (22 items across builder + Companion), plus a re-raise note folded into FR-12 (lazy-load vs full coverage). Every 2026-07-16 wave above is shipped.

**Prioritisation (agreed 2026-07-18).** Fresh waves across the whole open set. Cheap-and-correct first, epics last; each wave aims to verify in roughly one regression cycle. Six items carry small design decisions (FR-18, FR-20, FR-21, FR-22, FR-31, FR-35), resolved as we reach each item, not up front.

- **Wave A, quick wins + the one real bug:** BUG-12 (Runt granted spell, the only correctness item), BUG-13, BUG-15, FR-19, FR-22, FR-18, FR-28, FR-27, FR-30, FR-31.
- **Wave B, UX with more logic:** FR-24 (AP tracker), BUG-14 (search order), FR-29 (Android back), FR-20 (picker order), FR-9 (ancestry-trait slots, carried over ready).
- **Wave C, rules-browser structure pass (one coherent job):** FR-32, FR-33, FR-34, FR-35.
- **Wave D, epics + bigger design:** ~~FR-25 (Damage Calculator)~~ DONE v1 2026-07-19, ~~FR-23 (Stamina Regen)~~ DONE, ~~the BUG-16 auto-heal + data-model unification~~ DONE 2026-07-19 (grants-only; see the GRANTS-ONLY detail below), FR-12 + FR-13 together (full coverage + live legality) - **GO, planned as a phased epic 2026-07-19 (Darryl chose full completeness + refactor-first; the keep/defer question is resolved). Decided architecture in `FR12_PLAN.md`.** The old "questionable value + 0.11 treadmill" con was found to be mostly self-inflicted (the treadmill root = each class is a code change because `catalog_build.py` generates the spine FROM `build_engine.CLASS_TABLES`); Phase 0 inverts that so classes become data-only. Staircase: Phase 0 (data-drive the engine, = FR-12.0, DONE 2026-07-19 - `class_spines.yaml` is now the authored source of truth, `CLASS_TABLES` retired) -> Phase 1 (sourcing machinery + FR-13a, NEXT) -> Phase 2 (full spell/maneuver legality data) -> Phase 3 (FR-12 class coverage) -> Phase 4 (subclass/ancestry breadth) -> Phase 5 (mobile picker UX). [FR-21 was in this bullet as "nested collapsibles"; it shipped as category sub-headers in the FR-36 + FR-21 bundle, so it is done, not pending.]
- **Parked:** FR-26, FR-4 (needs scoping), FR-11.

Starting with Wave A (Darryl's call 2026-07-18).

**Wave A SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-18 (built on baseline `33a9119`; origin head now `7018b69 "Wave A: BUG-12 granted-spell on sheet + 9 quick wins"`; Darryl ran sync/commit/push + Chrome live-verify).** All ten items done in two passes. Builder pass (`tools/builder_build.py` -> regenerated `builds/builder.html`): **BUG-12** the one real correctness item - `sheet()` now includes granted spells from the `spell_tagged` slot (Runt's Psychic Tendrils from Beyond now lists on his sheet, verified via a staged BuilderAPI call returning 6 spells) and the decision-row label maps `spell_tagged` -> "spell" so the internal kind no longer leaks; **FR-18** badge is now "v0.10.5" (inline `text-transform:none` so the lowercase v survives the badge's uppercase rule); **FR-19** the three cryptic wlabel pills replaced by one "every level-up choice"; **FR-22** the `#srcinfo` "sources: N fetched, N baked" footer element and its population removed. Companion pass (`companion-src/template.html` + `build.py`, auto-deploys via the Pages Action on push): **BUG-13** a new `diceRow()`/`dropLowest()` marks EVERY kept die (radiant) and strikes the dropped ones, for both drop-N and d20 ADV/DisADV (tie-safe, verified in node: 4d6-drop-1 on [3,3,6,5] drops one 3, keeps 14); **BUG-15** `cursor:pointer` on `.pill` (used only by condition pills); **FR-27** the rules-browser Maneuver entries now get the same `spelldiv` divider as spells (29 entries, keyed on the `<li>Cost:` cue; the one non-costed "Heroic Pass Through" enhancement is correctly skipped; other long lists left for a later review); **FR-28** a Reset button clears Count/Drop/Mod/ADV back to defaults (leaves history, which Clear handles); **FR-30** Skills & Trades is now a `<details>` matching the accordions; **FR-31** every `<details open>` flipped closed (5 in template + TAN_ACCORDIONS in build.py) so all collapsible sheet sections, Skills & Trades included, start collapsed. Verified in a fresh `/tmp` clone: catalog_verify **90/90 zero deltas**, builder_verify **PASS exit 0**, builder inline JS `node --check` OK, regenerated builder.html cp'd clone->mount **sha256 MATCH**; player Companion build ends `</html>`, inline JS `node --check` OK, no `data:image/jpeg;base64`, `gm files: 0` (no GM leak). **Pushed in `7018b69`:** `tools/builder_build.py`, `builds/builder.html`, `companion-src/template.html`, `companion-src/build.py`, `builds/BACKLOG.md` (which also carried the earlier uncommitted 2026-07-18 backlog-intake doc). The Pages Action rebuilt the Companion; Darryl Chrome-verified. NOTE: this very status-flip (awaiting-push -> pushed 7018b69, across the table cells + this note) is a fresh uncommitted doc edit on the mount and will ride the NEXT commit, per the usual pending-doc-flip pattern. **Remaining:** Wave B (FR-24, BUG-14, FR-29, FR-20, FR-9), Wave C (FR-32/33/34/35), Wave D epics (FR-25, FR-23, FR-12+FR-13, FR-21).

**Wave B pass 1 (the Companion three) SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-18 (built on origin head `7018b69`; Darryl ran sync/commit/push + Chrome live-verify; new origin head is one commit past `7018b69` - run `git fetch` next thread to read the SHA).** Split Wave B along the app boundary (Darryl's call): a Companion pass first (FR-24, BUG-14, FR-29), then a builder pass (FR-20 after its design call, FR-9) later. All three are `companion-src/template.html`-only, so the deploy Action rebuilds the Companion from source on push and no built HTML is committed; `builds/builder.html` is untouched. **FR-24** the AP tracker now labels "AP avail: N" (N = unused AP, in gold) and the pips deplete leftmost-first (spent iff `i < 4-ap`; click sets `ap = i<4-ap ? 4-i : 3-i`), matching the ticket's "deplete left-to-right"; New turn still refills all 4. **BUG-14** rules search now takes the top 30 by score then renders them in rulebook (document) order (`display=top.slice().sort((a,b)=>a[1]-b[1])`) so hits from the same area group together, while `_results` stays score-ordered so the `searchFor` fallback still opens the best match. **FR-29** opening a rule doc does `history.pushState`, and a new `popstate` listener closes the doc (returns to the search list, or to the origin sheet when the ↩ Sheet path is taken) instead of the Android/browser Back button leaving the app; the on-screen ← Search / ↩ Sheet buttons route through `history.back()` so the pushed entry stays in sync. Verified in a fresh `/tmp` clone at `7018b69`: catalog_verify **90/90 zero deltas**, builder_verify **PASS exit 0** (builder untouched), rebuilt Companion ends `</html>`, inline JS `node --check` OK, `gm files: 0`, no `data:image/jpeg;base64`; canaries present in the built HTML; node behavioural checks confirm the pip depletion/toggle and the doc-order-vs-score-order split. **Pushed:** `companion-src/template.html` + `builds/BACKLOG.md` (with the earlier at-a-glance table split + the pending Wave A status flip); the Pages Action rebuilt the Companion and Darryl Chrome-verified. NOTE: this very status flip (awaiting-push -> pushed) is a fresh uncommitted mount edit that rides the NEXT commit, per the usual pending-doc-flip pattern. **One judgment call:** the numeric AP count is shown in gold `<b>` beside the "AP avail:" label (the ticket said "followed by the count of unused AP" without specifying styling). **Remaining Wave B:** FR-20 (needs a picker-order design call), FR-9 (ancestry-trait slots, the meaty engine/ledger item) - the builder pass.

**Wave B pass 2 (the builder pass) BUILT + VERIFIED in a fresh clone, AWAITING PUSH 2026-07-18 (baseline origin head `36ec33a` "Wave B pass 1"; builder-only, so the Companion is untouched and catalog_verify stays 90/90 by construction).** Both items are `tools/builder_build.py` -> regenerated `builds/builder.html`, plus harness sections in `tools/builder_verify.py`. No ledger, catalog, or `companion-src` change (git status = exactly 3 files: `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`).

- **FR-20 (picker order) DONE.** Darryl's picker-order call (2026-07-18): **attributes -> class/subclass -> ancestry -> resources**. Implemented as a stable render-order reorder inside `_decisions()` (`_reorder_decisions` + the `FR20_CAT` rank map): each level's top-level pickers sort by category rank (0 attributes / 1 class-structure: subclass, pact_boon, discipline, spell_school, talent, path, class_feature(s), bound_weapon_options / 2 ancestry_trait / 3 resources: spell, maneuver, skill/trade carriers + children); cross-level order untouched; **grant-child rows (GC#...) inherit their parent block's rank + sequence so they stay glued directly under the parent** (Xanwyn's 2 runes under his subclass, Scaletrix's 2 metamagic under her talent, Runt's Psychic spell under his subclass) rather than migrating to the resources section. Pure display order: no id/engine/budget change, so all first/last-of-a-slot harness reads stay valid (within-slot order preserved). Harness section **(21)** locks it: for all six PCs the top-level ranks are non-decreasing per level, every GC child is glued under its parent block, and the two cross-category glue cases + Runt L1 render in the exact expected sequence.
- **FR-9 (ancestry slots) DONE - Option A (polish + lock), Darryl's call.** Finding: the machinery the ticket described is **already built** - each ancestry trait is its own editable picker (all catalog options), there is a `+ Add trait` control (`add_trait`) + per-row remove, picking a trait **auto-applies its catalog cost** (0/1/2 or a negative-refund drawback) via `_set_trait`, and the **sum-to-budget is enforced** by the engine (`Ancestry points: N spent vs M budget`). Runt's 7 Giantborn traits were already itemised in `runt.yaml` (Wave 1). So Option A adds the allocator-style polish without an engine/ledger re-plumb: (1) a new `_anc_budget()` mirrors the engine's spent/budget and is surfaced in `state()` as `anc_spent`/`anc_budget`; the JS renders a live **"Ancestry points: N of M spent"** readout by the `+ Add trait` control (green balanced / amber "N to spend" / red over budget); (2) an **auto ready-slot** (id `cg:trait:+`) shows ONE empty ancestry picker while under budget and no open slot exists, so the common case needs no button - it flows through the normal decision renderer and `set_decision` materialises a real trait on first pick, then re-derives whether another auto slot is warranted (chains like the skills allocator). Guarded: the auto slot is suppressed at budget (all six canon ledgers) and whenever an undecided slot already exists - which is exactly what keeps the trips/scratch harness safe (they add a real undecided slot before reading). `undecAt` skips auto rows so the empty ready-slot does not nag. Harness section **(22)** locks it (15 checks: six ledgers at-budget clean; under-budget one auto slot + readout numbers + engine problem; picking the sentinel materialises + rebalances; the guard; page furniture).

- **BUG-16 (maneuver/spell edit-only) DONE - safety half, from Darryl's live-verify.** He noticed the L2/L4 maneuver rows carried a red x (builder-provenance note) but the L1 ones + ancestry did not, tested it, and found deleting a maneuver silently and unrecoverably dropped Runt below his granted count. Fixed by making maneuver/spell budget slots edit-only (dropped their `removable` clause; ancestry stays removable because it self-heals). The **auto-heal ready-slot half is DEFERRED**: I measured that the flat-pool+grants model represents grants inconsistently (granted maneuvers ARE flat entries, granted spells are NOT), so entry-count is "short" of the budget by the grant amount on 4 PCs (bonan/runt/scaletrix/xanwyn spells) and a naive auto-fill would show phantom slots. A reliable auto-heal needs the deferred data-model unification; with deletion removed, a shortfall can no longer be created via the UI, so edit-only already gives the safe/consistent end state. Harness section (23). See the BUG-16 detail below.

Regression: pristine `git clone` at `36ec33a` + the three edited source files -> `catalog_verify` **90/90 zero deltas**, `builder_verify` **PASS exit 0** (new sections 21 + 22 + 23; the old check_addlevel "level slots removable" assertion updated to "NOT removable" for BUG-16), inline JS `node --check` OK, regenerated `builds/builder.html` (2,460,087 bytes) cp'd clone->mount **sha256 MATCH**, no em-dashes added. **Files to sync/commit/push:** `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md` (these notes + the table flips + the standing Wave B pass 1 awaiting-push -> pushed flip that was still uncommitted on the mount). **Live-verify after push (Chrome):** open the builder for any PC and confirm each level's pickers read attributes -> class -> ancestry -> resources with runes/metamagic still under their parent; drop a PC under its ancestry budget and confirm the "Ancestry points: N of M spent" readout + the single ready empty slot; and confirm the maneuver/spell rows no longer carry a red x (edit via the dropdown instead). **Wave B COMPLETE (pending Darryl's confirm that BUG-16 edit-only suffices, else the auto-heal follow-on).** Next: Wave C (rules-browser structure pass: FR-32/33/34/35, FR-32 + FR-35 need scope calls).

**FR-36 + FR-21 (the "tame long level lists" design-call bundle) BUILT + VERIFIED in a fresh clone, AWAITING PUSH 2026-07-18 (baseline origin head `a1096e9` "BUG-16 ... edit-only" - which CONFIRMS the Wave B pass 2 push landed; Darryl confirmed the edit-only BUG-16 fix suffices, so the auto-heal follow-on stays deferred). Builder-only, so the Companion is untouched and catalog_verify stays 90/90 by construction.** Three source files: `tools/builder_build.py` (the `_reorder_decisions` cat tag in `API_PY` + the `.dec.catN` / `.deccat` CSS + the `rowHTML` category class / editable-branch note + the render-loop sub-header grouping, all in the `TEMPLATE`), `tools/builder_verify.py` (new sections 24 + 25), regenerated `builds/builder.html`. Design calls (all Darryl's, all the recommended option): FR-36 palette = CB-safe blue/purple/teal/amber by FR20_CAT rank, category colour REPLACES the amber `.newlvl` border (builder-touched now rides the amber note text, now shown on editable rows too); FR-21 = light category sub-headers for levels with >=5 rows (chargen L1), NOT nested collapsibles. Regression in a pristine `git clone` at `a1096e9` + the edited sources: `catalog_verify` **90/90 zero deltas**, `builder_verify` **PASS exit 0** (sections 24 + 25 added), inline JS `node --check` OK, regenerated `builds/builder.html` cp'd clone->mount **sha256 MATCH**, no em-dashes added. **Files to sync/commit/push:** `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md` (these notes + the table flips). Suggested message: `FR-36 category accent colours + FR-21 category sub-headers`. **Live-verify after push (Chrome):** open the builder for any PC and confirm each level's decision rows now carry a coloured left border by category (blue attributes / purple class / teal ancestry / amber resources) with runes/metamagic sharing their subclass's colour; open Runt (or any char) and confirm his Level 1 shows the Attributes / Class / Ancestry / Resources sub-headers while a short level-up level stays flat. Next: Wave C (rules-browser structure pass: FR-32/33/34/35, FR-32 + FR-35 need scope calls).

**Backlog-review pull-out (small builder pass) SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-18 (baseline origin head `5b7f0f0`; new origin head `7676679` "FR-4 display-name rename for canon PCs + FR-21 planning-note cleanup"; Darryl ran sync/commit/push + Chrome-verified the rename card on Scaletrix, with the EDITING CANON bar showing).** After a full backlog sweep, Darryl pulled four items out of the waves to reprioritise: FR-23 (Stamina Regen) and FR-25 (Damage Calculator) to sit as their own later passes, the display-name half of FR-4 as a quick win now, and the stale FR-21 planning note to clean up. This pass ships the two cheap-and-correct ones together (builder-only + doc), leaving FR-23 and FR-25 as standalone future work. **FR-4 display-name slice:** canon/loaded characters now get a slim name-only rename card wired to `set_meta('character', ...)` through `refresh()` (dirty + EDITING CANON bar); see the FR-4 detail below. Harness section (26). **FR-21 note cleanup:** the Wave D prioritisation bullet no longer lists "FR-21 (nested collapsibles)" as pending (it shipped as sub-headers in the FR-36 + FR-21 bundle); the To Do table already had it in Done. Regression in a pristine `git clone` at `5b7f0f0` + the edited sources: `catalog_verify` **90/90 zero deltas**, `builder_verify` **PASS exit 0** (new section 26), inline JS `node --check` OK, regenerated `builds/builder.html` cp'd clone->mount **sha256 MATCH**, no em-dashes added. **Files to sync/commit/push:** `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md`. Suggested message: `FR-4 display-name rename for canon PCs + FR-21 planning-note cleanup`. **Live-verify after push (Chrome):** open the builder for any canon PC (e.g. `?char=runt`), confirm a "Character / display name" card with a name field appears above Decisions, edit it and confirm the title/sheet name update, the EDITING CANON bar shows, and the export filename follows the new name while the deep link still resolves by handle. Next: FR-23 (needs a scope call), then FR-25 (design-first epic), or Wave C.

**Cleanup batch (post-FR-23 polish) SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-18 (baseline origin head `3dd3cb9`; new origin head `0a27495 "Cleanup: EDITING CANON colon + Stamina Regen roundnote de-dup + FR-23 doc-flip"`; Darryl ran sync/commit/push + Chrome-verified the EDITING CANON bar and the trimmed roundnotes).** Three trivial follow-ups bundled with the pending FR-23 doc-flip. (1) **FR-23 doc-flip** itself: the Done-table row + the FR-23 detail entry flipped AWAITING PUSH -> PUSHED `3dd3cb9` (the standing pending edit carried over from last thread). (2) **EDITING CANON em-dash:** the builder canonbar (`render()` in `tools/builder_build.py`) rendered a literal `&mdash;` against the no-em-dashes-anywhere convention; swapped for a colon; regenerated `builds/builder.html`. (3) **Stamina Regen roundnote de-duplication** (`companion-src/template.html`): the per-PC `roundnote` prose for Tan/Runt/Xan restated the Stamina Regen trigger that the FR-23 structured line now shows, so the duplicated trigger text was trimmed, keeping the unique bits (Tan's "SP refills fully at combat end"; Runt's Warlock Bargains + the casting-and-wrestling self-fuel note; Xan's Spellstrike/Blink Blade/Smite). No engine/catalog/ledger change, so catalog_verify stays 90/90 by construction. Regression in a pristine `git clone` at `3dd3cb9` + the edited sources: `catalog_verify` **90/90 zero deltas**, `builder_verify` **PASS exit 0**, inline JS `node --check` OK, regenerated `builds/builder.html` cp'd clone->mount **sha256 MATCH**, rebuilt Companion ends `</html>` / `gm files: 0` / no `data:image/jpeg;base64`, no em-dashes added. **Files:** `tools/builder_build.py`, `builds/builder.html`, `companion-src/template.html`, `builds/BACKLOG.md`. Suggested message: `Cleanup: EDITING CANON colon + Stamina Regen roundnote de-dup + FR-23 doc-flip`. **Live-verify after push (Chrome):** open the builder, edit a canon PC to enter EDITING CANON, and confirm the warning bar reads "EDITING CANON:" with no em-dash; open the Companion for Tan/Runt/Xan and confirm the roundnote no longer repeats the Stamina Regen trigger (still shown as the structured line under SP). NOTE: this AWAITING PUSH -> PUSHED flip (the opening line of this note) is now the one doc-only uncommitted mount edit that rides the NEXT commit, per the usual pending-doc-flip pattern.

**FR-25 Damage Calculator v1 SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-19 (origin `44e6e9c "FR-25 Damage Calculator v1 (catalog-driven; Companion)"`; the carried cleanup-batch doc-flip landed in the same commit). Two follow-ups also PUSHED + CHROME-VERIFIED: a UI-polish round (`b17793f`) and a data-correction round (`a349201`, now the origin head) - see the notes below.** Baseline was origin head `0a27495`. Design-first epic: the three scope calls were settled with Darryl (all the recommended option) - **single-hit total** (v1), **catalog-driven** data, **one-tap pull** from the Roll section. See the FR-25 DESIGN block above for the full spec. Built as a new collapsible "Damage Calculator" card between the Roll card and Skills & Trades, structured on DC20's own Adding Damage Order (`core-rules.md` l.2376): a Base stepper (defaults to the PC's weapon/spell base, editable), a hit-grade chooser (Hit/Heavy/Brutal/Beyond = +0/1/2/3), a Crit toggle (+2, bypasses DR), the PC's per-character add-ons (toggles for fixed +N like Rage/Powerful/Death's Toll/imbue; capped steppers for Smite +2/SP, MP->dmg +2/MP cap 2, generic Damage +1), a live running total with a breakdown line, and a "Pull from last roll" button that copies the grade + crit the Roll section computed. **Data model:** NEW `builds/catalog/damage_addons.yaml` = a reusable `defs` library keyed to a per-character `characters` assignment (add-ons are per-character since they come from items/spells/subclass, so the two Spellblades differ - hence handle-keyed, unlike the class-keyed stamina_regen.yaml). A shared `build_engine.damage_addons(handle, cat)` resolves each PC's config (catalog-agnostic, data passed in), so builder and Companion can't diverge. `companion-src/build.py` bakes it into `PARTY_DERIVED`; `template.html` merges it over `CHARS` and renders/computes the card; `roll()` now stashes the last Attack/Spell-Check grade+crit in `_lastRoll` for the pull button. **Scope (v1):** single-target hit only; AoE/per-target math (Whirlwind, Fireball/Breath area, Chain), target Damage Reduction, and ruling-dependent engines (Rolling Wild Fire) are a possible v2; accuracy-only riders (Call of the Grave, Hasty Bargain) are excluded by design (not damage). Per-PC add-ons: tan {smite, powerful, mp_to_damage, deaths_toll}, xan {smite, Umbral imbue, mp_to_damage}, runt {mp_to_damage, Lightning imbue}, min {gen_damage, battlefield}, bonan {rage, gen_damage, erupting}, scale {mp_to_damage, powerful}. **Harness:** `catalog_verify.py` NEW section (5) - every char's add-on ids resolve to a def, steppers/toggles are well-formed, mp_to_damage=+2/MP cap 2 and smite=+2/SP are grounded in combat.md l.1807 + the Spellblade Smite reading, the hit-grade/crit constants are grounded in core-rules.md, and the six resolved assignments match the expected sets (Smite Spellblade-only, Rage Barbarian-only). Regression in a pristine `git clone` at `0a27495` + the edited sources: `catalog_verify` **90/90 zero deltas + new section (5) PASS**, `builder_verify` **PASS exit 0** (engine change is additive, builder untouched by the calc), rebuilt Companion ends `</html>` / `gm files: 0` / no `data:image/jpeg;base64`, inline JS `node --check` OK; regenerated `builds/builder.html` (the baked engine changed) cp'd clone->mount **sha256 MATCH**, its inline JS `node --check` OK; a Python mirror of the total math checks out (Tan heavy+smite2+powerful+crit = 10; Bonan brutal+rage+gen1 = 6; Scale hit+mp2+powerful = 6); no em-dashes added. **Files to sync/commit/push:** NEW `builds/catalog/damage_addons.yaml`, `tools/build_engine.py`, `companion-src/build.py`, `companion-src/template.html`, `tools/catalog_verify.py`, regenerated `builds/builder.html`, `builds/BACKLOG.md` (this note + the table move + the design-block flip + the carried-over cleanup-batch doc-flip). Suggested message: `FR-25 Damage Calculator v1 (catalog-driven; Companion)`. **Live-verify after push (Chrome):** open the Companion for a PC, expand Damage Calculator, set a base + hit grade + an add-on and confirm the total; roll an Attack with a "vs Def" set, then tap "Pull from last roll" and confirm the grade+crit copy in. NOTE: this note's AWAITING PUSH -> PUSHED flip will be the pending doc-only edit riding the NEXT commit after this one lands.

**GRANTS-ONLY UNIFICATION + maneuver/spell AUTO-HEAL (BUG-16 full fix) BUILT + VERIFIED in a fresh clone, AWAITING PUSH 2026-07-19 (baseline origin head `a349201`; Darryl picked the Wave D data-model strand to run on its own before the FR-12/13 conversation, and chose full scope = unify + auto-heal, and "surface" for Scaletrix's gaps).** The maneuver/spell data model was inconsistent three ways: (1) Runt's FIXED pact-boon maneuvers (Cleave/Pathcarver L1, Brace/Side Step L4) were DUPLICATED into the flat pool AND their `granted_maneuvers` lists; (2) Bonan/Xanwyn's granted spells were bundled into composite strings ("Command, Charm, Frost Bolt (order unknown)"); (3) Scaletrix had ~4 genuinely-unrecorded Arcane spells hidden inside composite text. The engine derives "Maneuvers/Spells known" as a BUDGET (class table + path + grants), never by counting flat entries, so the fix is a ledger/builder representation change the engine is indifferent to - **66/66 preserved by construction** (verified 0 engine problems across all six).

**Model (grants-only):** FIXED grants (specific per boon) live ONLY in `granted_<resource>` lists; CHOICE grants ("+N of your choice": Martial Expansion, MC Bard, Innate Power, SSI, Spellcaster path, class table) are flat free picks. So flat_free + granted_lists == the budget, consistently, for both resources.

**Ledger edits (4 files):** `runt.yaml` - removed Cleave/Pathcarver from chargen flat + Brace/Side Step from the L4 flat slots (all four stay in their boons' `granted_maneuvers`). `bonan.yaml` - de-bundled the 3-spell composite into Command/Charm/Frost Bolt. `xanwyn.yaml` - de-bundled Death Bolt/Regenerate. `scaletrix.yaml` - cleaned the two composite spell strings to single names (Regenerate, Dispel Magic); his 4 genuinely-unrecorded Arcane spells now surface as auto-heal ready slots (**ASK DAMO** to name them; documented in a ledger comment).

**Builder (`tools/builder_build.py` -> regenerated `builds/builder.html`):** (a) NEW helpers `_res_budget`/`_res_have`/`_res_has_undecided` + an FR-9-style auto ready-slot in `_decisions` (ids `cg:man:+`/`cg:spell:+`): when named picks < budget it shows ONE ready picker, chaining one at a time; `set_decision` materialises a real chargen pick on selection. Advisory only (legal-but-unfinished), so it raises NO problem and the baseline stays clean. (b) A live **"Maneuvers/Spells: N of M recorded"** readout (`#resreadout`, meta `man_have/man_budget/spell_have/spell_budget`), shown only when under budget. (c) FIXED grants now render as **read-only "granted (fixed) by <boon>" rows** (so Runt's 4 boon maneuvers stay visible after leaving the flat pool - they are not editable free pickers). (d) **RETIRED the one-click "expand into per-level slots" reconcile** (`expand_composite` + `_total_granted`/`_granted_at_level`/`_parse_picks` + the `data-expand`/`t.expandable` JS): it flattened fixed grants into the flat pool - exactly the double-count removed here - and is superseded by the auto-heal. The single-value composite REPLACE dropdown is kept.

**Harness:** `builder_verify.py` NEW section **(28)** (per-PC gap == expected with Scaletrix 0/4; Runt's read-only granted rows + no duplicate maneuver; Scaletrix's one auto spell slot + no problem + 6/10 readout; materialise + chain to close all 4; undecided suppresses the auto slot; page furniture; expand_composite gone) + updated sections 11/21/22/23 for the new representation. **Regression in a pristine `git clone` at `a349201` + the 6 edited sources:** `catalog_verify` **90/90 zero deltas** (ledger legality intact), `builder_verify` **PASS exit 0** (incl. new section 28), inline JS `node --check` OK, regenerated `builds/builder.html` cp'd clone->mount **sha256 MATCH**, rebuilt Companion ends `</html>` / `gm files: 0` / no `data:image/jpeg;base64` and its **PARTY_DERIVED is byte-identical** (5303 bytes - the ledger edits don't touch engine-derived numbers), no em-dashes added. **Companion needs no source change** but the deploy will rebuild it (the push touches `builds/**` + `builds/*.yaml`); output is functionally unchanged.

**Files to sync/commit/push:** `builds/runt.yaml`, `builds/bonan.yaml`, `builds/xanwyn.yaml`, `builds/scaletrix.yaml`, `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md` (these notes + the BUG-16 row flip). Suggested message: `Grants-only maneuver/spell unification + auto-heal (BUG-16 full fix); reconcile retired`. **Live-verify after push (Chrome):** open the builder for **Runt** and confirm his 4 boon maneuvers show as read-only "granted (fixed) by Pact Weapon/Pact Armor" rows (no duplicates, no free-pick dropdown on them) and the free maneuvers are editable; open **Scaletrix** and confirm a "Spells: 6 of 10 recorded" readout + one ready `(undecided)` spell picker, that picking it adds the spell and reveals the next, and that no red problem shows. **ASK-THE-PLAYERS add:** Damo - name Scaletrix's 4 unrecorded Arcane spells (2 Innate Power Intuitive + 2 Spellcaster-path). NOTE: this note's AWAITING PUSH -> PUSHED flip rides the NEXT commit, per the usual pending-doc-flip pattern.

**FR-13a SLICE 1 (sourcing machinery + Scaletrix sourced) SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-19 (origin `34ea490`; 8 files; Darryl confirmed all 10 Scaletrix spells render, the two under Innate Power are Arcane-filtered).** Darryl supplied Scaletrix's sheet screenshot, which resolved the ASK-DAMO item: his 10 spells are 5 PRIMAL (Wild Guard, Primal Hide, Fireball, Fire Bolt, Regenerate) + 5 ARCANE (Command, Disintegrating Beam, Gravity Well, Dispel Magic, Telekinesis; the sheet's "Telekenesis" is a typo). Two parts. **(1) Data (`builds/scaletrix.yaml`):** all 10 spells named + sourced to their granting feature - 5 Primal via the Druid class table (4 chargen L1 + Regenerate moved to L3 where the spine grants its post-L1 spell); the 5 Arcane distributed across the exactly-5 Arcane grant slots: Command via the Fiendish Magic ancestry trait (flat chargen L1 for now, see slice 2), Disintegrating Beam + Gravity Well via Innate Power / Intuitive Magic (childed under the L2 talent, source-filtered), Dispel Magic + Telekinesis via the two Spellcaster-path ranks (flat L2/L4, Sorcerer = Arcane). Old mis-source (Dispel Magic as "class table +1") corrected. Engine count unchanged (10); PARTY_DERIVED byte-identical (Companion untouched). **(2) Machinery (the reusable bit): a SOURCE-constrained spell grant-child, the source sibling of the FR-8 slice-5 tag-constrained one.** `build_engine`-side untouched; in the builder (`tools/builder_build.py`): new `_spell_grant_source(parent)` reads a `spell_access: {source, schools}` on the granting entry, `_spell_sourced_options()` filters spells.md by Source (+ optional schools), `_grant_children` renders N `spell_sourced` child pickers for such a grant (Innate Power -> 2 Arcane-filtered pickers reading `granted_spells`), and `_res_have`/`_res_has_undecided`/the chosen-set/sheet-list/JS-slot-label all learn the new slot + ancestry-trait `granted_spells` harvesting. `catalog_verify` now harvests ancestry-trait granted spells and counts Spellcaster-path ranks as Arcane grant slots (Scaletrix off-source 5 <= 5); `builder_verify` section (28) repointed off Scaletrix's now-closed gap (synthetic-gap fixture keeps the auto-heal chaining covered) + slice-5 boundary now asserts the FR-13a positive case (2 Arcane-only spell_sourced children). Verified in a fresh clone at `2b80f98` + the edited sources: catalog_verify **90/90**, builder_verify **PASS exit 0**, builder.html cp'd clone->mount **sha256 MATCH**, build.py clean (PARTY_DERIVED unchanged), no em-dashes added. **SLICE 2 (deferred):** child Command under the Fiendish Magic ancestry trait (needs an ancestry-trait grant-child parent path in `_set_grant_child`), model the Sorcerous Origin sub-choice as an explicit node, and extend source-filtering to the flat path picks. **Cosmetic driver for slice 2 (Darryl's live-verify catch):** the three Arcane spells still recorded as FLAT picks (Command L1, Dispel Magic L2, Telekinesis L4) render with a "(current, off-list)" label, because a flat spell picker filters to the class Source (Druid = Primal) and these are Arcane (legal only via a grant slot), so the off-list guard keeps them visible-but-tagged. Not a correctness issue (build valid, harness green) - the guard is the same one that fixed the old blank-Dispel-Magic bug. Slice 2 removes the tag by giving each its own source-constrained picker: child Command under the ancestry trait, and source-slot the two path spells under their Spellcaster-path picks (fiddly because path spells come from the caster-rank count, not a `grants:{spells}` entry). Disintegrating Beam + Gravity Well already read clean because they are `spell_sourced` children (Arcane-filtered). **Files to sync/commit/push:** `builds/scaletrix.yaml`, `tools/builder_build.py`, `tools/catalog_verify.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/runt.yaml` (stale BUG-7 note cleanup), `builds/BACKLOG.md`, `builds/FR12_PLAN.md`. Suggested message: `FR-13a slice 1: source-constrained spell grant-child + Scaletrix fully sourced (10 spells from sheet); tidy stale runt BUG-7 note`. **Live-verify (Chrome):** open Scaletrix - all 10 spells show as filled pickers (no ready-slots), and the two spells under Innate Power (Disintegrating Beam, Gravity Well) are dropdowns offering Arcane spells only.

**FR-13a SLICE 2 (finish the sourcing machinery) BUILT + VERIFIED, AWAITING PUSH 2026-07-19 (baseline origin `34ea490`; builder-only, so the Companion is untouched and catalog_verify stays 90/90 by construction). FR-13a is COMPLETE on push.** Delivers the stated payoff: all THREE flat Arcane picks (Command, Dispel Magic, Telekinesis) lose the "(current, off-list)" tag, each rendered by its own source-constrained picker. Three parts. **(a) Command childed UNDER the Fiendish Magic ancestry trait.** New ancestry-trait grant-child path: `_decisions` now calls `_grant_children(t, 'cgtrait:<i>', ...)` after each chargen ancestry-trait row (so the child glues under the trait via the FR-20 anchor), and `_set_grant_child`/`_grant_child_entry` learn the `cgtrait:<i>` parentref (-> `chargen.ancestry_traits[i]`). In `builds/scaletrix.yaml` Command moved out of the flat `chargen.spells` list onto the trait as `granted_spells: [Command]` + `spell_access: {source: Arcane, schools: [Elemental, Enchantment]}`, so it renders as an Arcane/Enchantment-filtered picker (38 opts). **Engine count is unchanged (10):** the engine derives spells as `table + caster + grants` (never from the flat lists), and the trait already carried `grants: {spells: 1}`, so re-homing Command is display-only; `_res_have` was already harvesting ancestry-trait `granted_spells` (slice-1 prep), so the budget still balances. **(b) the two Spellcaster-path spells (Dispel Magic L2, Telekinesis L4) source-filtered in place.** A flat `spell` entry carrying a CANONICAL source (`source: Arcane`) now renders source-filtered to that Source via a new `_all_sources()` guard + a `spell_source=` arg threaded through `_dec` from the level loop, so the two Arcane path spells are legal pickers (92 opts, no off-list) instead of Primal-filtered off-list rows. Free-text provenance sources ("class table +1", "Spellcaster path +1 spell" on minimus/bonan) are NOT canonical and fall through to the class list unchanged. **Design call (flagged for Darryl):** the plan said "source-slot the path spells UNDER their path picks"; I filtered them in place instead of childing under the path, because path spells derive from the caster-rank COUNT (not a `grants:{spells}` entry), so childing them would need a fake grant that the engine would double-count. In-place source-filtering delivers the identical visible payoff (off-list cleared) with zero engine risk. **(c) the Sorcerous Origin sub-choice is now an EXPLICIT node.** A parent carrying a `sorcerous_origin` dict (Scaletrix's Innate Power) renders one `source_choice` picker (Arcane/Divine/Primal, labelled "sorcerer source") childed under the talent above its spell children; `_spell_grant_source` reads `sorcerous_origin.chosen_source` first (falling back to `spell_access.source`), so the node DRIVES the Intuitive-Magic children's source filter - re-picking it to Primal resets the 2 children to undecided and re-filters them to Primal. The node is invisible on the char sheet (the sheet's Features & abilities uses an explicit slot allow-list; spells come from the spell/spell_tagged/spell_sourced harvest). **Edge case hardened:** `_set_trait` now drops the spell-grant provenance (grants.spells / granted_spells / spell_access / sorcerous_origin) when a spell-granting ancestry trait is re-picked to a different trait, so a stale Command child (and phantom spell in the count) cannot linger (honest MISMATCH surfaces if you edit that canon pick, as intended). **Verified in a fresh clone at `34ea490` + the edited sources:** catalog_verify **90/90 zero deltas**, builder_verify **PASS exit 0** (section 28 slice-1 assertion re-scoped to Innate Power's `GC#L2:0#spells#` children; 7 NEW slice-2 checks: (a) Command is a `cgtrait:` grant-child + writes granted_spells; (b) Dispel Magic + Telekinesis source-filtered with no off-list + free-text-source not treated canonical + NO Scaletrix picker carries an off-list tag; (c) explicit Sorcerous Origin node + re-pick resets/re-filters children), builder.html regenerated (2,508,954 bytes) cp'd clone->mount **sha256 MATCH**, inline JS `node --check` OK (via builder_verify), no em-dashes added. **Files to sync/commit/push:** `builds/scaletrix.yaml`, `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md`, `builds/FR12_PLAN.md`. Suggested message: `FR-13a slice 2: Command childed under Fiendish Magic ancestry trait + Sorcerous Origin explicit node + source-filtered flat path picks (all 3 off-list tags cleared); FR-13a complete`. **Live-verify (Chrome):** open Scaletrix - Command is a filled dropdown under the "Arcane Spell" ancestry trait (offering Arcane/Enchantment spells, no "off-list"); Dispel Magic + Telekinesis are filled Arcane dropdowns with no "off-list"; under Innate Power a "sorcerer source" picker reads Arcane above the two spell dropdowns, and flipping it to Primal blanks + re-filters those two to Primal.

**GRANTS-ONLY maneuver-type-filter round SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-19 (origin `aae3a9b`; 6 code/data files + BACKLOG; Darryl screenshot-confirmed Pact Weapon = Attack-only, Pact Armor = Defense-only).** Darryl's live-verify of the bug-fix round: Runt's maneuvers editable = correct, but the pact-boon pickers offered ALL maneuvers - a Pact Weapon grants "2 Attack Maneuvers of your choice" (classes.md l.3244) and Pact Armor "2 Defensive of your choice" (l.3269), so the pickers should be TYPE-constrained (this was deferred, never in committed scope - Darryl's good catch). Fix (all data-driven, the maneuvers catalog already tags every maneuver Attack/Defense/Grapple/Utility): added `maneuver_type` (Attack / Defense) to the two boons in `WARLOCK_PACT_BOONS` (`tools/catalog_build.py`) -> regenerated `builds/catalog/warlock.yaml`; `_grant_children` now filters each pact-boon maneuver grant-child's options to the boon's `maneuver_type` (keeping the current pick selectable if off-type). `catalog_verify` asserts the boon `maneuver_type` is a valid category + matches expected; `builder_verify` section (28) asserts Pact Weapon pickers offer Attack-only and Pact Armor Defense-only. Verified in a fresh clone at `cf1cb83`: catalog_verify **90/90 zero deltas**, builder_verify **PASS exit 0**, builder.html cp'd clone->mount **sha256 MATCH**, inline JS node --check OK, no em-dashes added (the one em-dash in the regenerated warlock.yaml header is the pre-existing "SCRIPTED -" boilerplate). Companion unaffected (no ledger/build.py change). **Files to sync/commit/push:** `tools/catalog_build.py`, `tools/builder_build.py`, `tools/catalog_verify.py`, `tools/builder_verify.py`, `builds/catalog/warlock.yaml`, `builds/builder.html`, `builds/BACKLOG.md`. Suggested message: `Grants-only: type-filter pact-boon maneuver pickers (Pact Weapon=Attack, Pact Armor=Defense)`. **Live-verify (Chrome):** open Runt - the two Pact Weapon maneuver dropdowns list only Attack maneuvers, the two Pact Armor ones only Defensive. **Scaletrix's multi-source spell sourcing is scoped OUT as NEW FR-13a (see To Do + detail below), Darryl's call 2026-07-19.**

**GRANTS-ONLY live-verify bug-fix round SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-19 (origin `cf1cb83`; builder-only, 3 files: `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`; ledgers unchanged).** Darryl Chrome-verified the grants-only push: Xanwyn/Bonan fine, but two real bugs on Runt & Scaletrix. **(1) Runt's boon maneuvers were LOCKED.** I had rendered `granted_maneuvers` as read-only rows on the assumption they were FIXED grants - but `classes.md` l.3244 (Pact Weapon = 2 Attack Maneuvers **of your choice**) / l.3269 (Pact Armor = 2 Defensive **of your choice**) means they are player-CHOICE, tied to the boon. Fix: render them as **editable grant-child pickers** (the exact FR-8 slice-5 shape), stored in `granted_maneuvers`, gated to `pact_boon` parents so flat-pool choice grants (Martial Expansion {maneuvers:2}, MC Bard {spells:2}) are untouched; `_apply_grants` now resizes `granted_maneuvers` for a pact boon (re-picking rebuilds/clears the child slots); `_res_has_undecided` also sees an undecided grant-child so the boon slot and the auto-heal slot never double up. Runt's 8 maneuvers are now all editable pickers (4 boon grant-children glued under the boon + 4 flat free). **(2) Scaletrix's "Dispel Magic" picker rendered blank.** It is an inferred/guessed pick ("or another of the Arcane set") that is not in his school-filtered options, so the `<select>` had no matching option. General fix in `_dec`: a picker always prepends its own current value as an "(off-list)" option if missing, so any inferred/off-list pick stays visible and correctable. **Harness:** section (28) updated (boon maneuvers = editable grant-children; off-list-current test), plus the check_replace_hatch + slice-5 boundary + FR-20 Runt-L1 assertions updated for the new grant-child rows. Regression in a pristine clone at `9196f80` + the 3 edited sources: catalog_verify **90/90 zero deltas**, builder_verify **PASS exit 0**, builder.html cp'd clone->mount **sha256 MATCH**, no em-dashes; Companion unaffected (no ledger/build.py change; PARTY_DERIVED unchanged). **Files to sync/commit/push:** `tools/builder_build.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md`. Suggested message: `Grants-only fixes: pact-boon maneuvers editable (of-your-choice); off-list picker value kept selectable`. **Live-verify (Chrome):** Runt - all 8 maneuvers are editable dropdowns (Cleave/Pathcarver under Pact Weapon, Brace/Side Step under Pact Armor, all changeable); Scaletrix - the Dispel Magic picker shows "Dispel Magic (current, off-list)" selected (not blank) and every spell picker has its value.

**FR-25 UI polish (Darryl's live-verify notes, on Bonan) SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-19 (origin `b17793f`; Companion-only, ONE file).** Five tweaks in `companion-src/template.html`: (1) the base-damage note ("Mace ...") now sits on the same line as the base stepper (dropped `.dmgrow .note{flex-basis:100%}` so notes flow inline and only wrap when there's no room); (2) the "Pull from last roll" button no longer wraps to a stack (`white-space:nowrap`); (3) add-on note text (Rage / Damage enhancement / Erupting Elements) rides the same line as its control, same CSS change; (4) a toggle add-on now **defaults ON when the sheet carries a matching active toggle** - so Bonan's Rage add-on pre-fills +1 when the RAGING toggle up top is on (generic: `S.t[id]` keyed by add-on id, only rage matches among our six); (5) the calculator now **preserves its inputs across same-character re-renders** (`renderDmg` rebuilds only when `dmg.char !== cur`, else `dmgSync()`), so fiddling AP pips / HP / conditions / toggles no longer wipes a set-up calc - it resets only on a character switch. Note: (4) is a default at (re)build, not a live mirror - toggling RAGING later won't retroactively flip the calc Rage (state is preserved by design), the player toggles the calc Rage themselves. Regression in a fresh `git clone` at `44e6e9c` + the edited template: only `companion-src/template.html` modified; `catalog_verify` **90/90 zero deltas**, `builder_verify` **PASS** (both untouched by a Companion-only change); rebuilt Companion ends `</html>`, `gm files: 0`, no `data:image/jpeg;base64`, inline JS `node --check` OK; no em-dashes added. **Builder untouched** (no engine/catalog change), so `builds/builder.html` is NOT regenerated. **File to sync/commit/push:** `companion-src/template.html` (+ `builds/BACKLOG.md` doc). Suggested message: `FR-25 calc UI polish (inline notes, Pull button, Rage default, preserve state)`. **Live-verify (Chrome):** open Bonan, expand Damage Calculator - notes should be inline, the Pull button one line, and with RAGING on up top the calc Rage should start +1; set up a calc then tap an AP pip and confirm it does NOT reset.

**FR-25 data corrections (Darryl's 2nd live-verify pass) SHIPPED + PUSHED + CHROME-VERIFIED 2026-07-19 (origin `a349201`, current head; Companion-side data + harness, `builds/builder.html` NOT touched - damage_addons.yaml is not baked into the builder).** All in `builds/catalog/damage_addons.yaml` (+ `tools/catalog_verify.py` section 5): **Bonan** - Erupting Elements removed from the calc (it triggers when he is HIT, not when he hits); generic **Damage enhancement cap 3 -> 2** (Stamina Spend Limit). **Minimus** - crossbow **base 1 -> 3**; base_note trimmed to just "Crossbow" (dropped the "his real value is granting allies attacks" line). **Runt** - Lightning Bolt **base 1 -> 2** (Powerful focus baked in); base_note trimmed (dropped "staff swings are minor"). **Tan** - added a **Spellstrike (Radiant Bolt +2)** toggle, and **split Smite**: the old +2/SP (which wrongly scaled the free enhancement per SP) is now `smite` = **+1 Bound damage per SP** plus a separate one-shot `smite_free` toggle = the **single free Damage enhancement Smite grants, no matter how many SP** (Darryl ruling). **Xanwyn** - added **Spellstrike (Umbral Bolt +1)** + the same Smite split. New defs: `smite_free`, `spellstrike`; `erupting` def removed. Engine/build.py/template.html all UNCHANGED by this correction (the calc UI renders generically; Spellstrike/free-enh are ordinary toggle add-ons; per-character bolt damage rides the existing override mechanism). Regression in a fresh `git clone` at `44e6e9c` + edited sources: `catalog_verify` **90/90 + section (5) PASS** (smite +1/SP, smite_free one-shot, gen_damage cap 2, Spellstrike Spellblade-only with Tan +2 / Xan +1, Min base 3, Runt base 2 all asserted), `builder_verify` **PASS** (builder untouched), Companion rebuild clean (`gm files: 0`, ends `</html>`, no image leak, JS `node --check` OK), corrected math mirror checks out (Tan greatsword2 + Heavy + Smite3 + free + Spellstrike = 9, not the old double-counted 11; Min brutal+gen2 = 7; Runt hit+mp2 = 6), no em-dashes. **Files to sync/commit/push (this + the polish round together):** `companion-src/template.html` (polish), `builds/catalog/damage_addons.yaml`, `tools/catalog_verify.py`, `builds/BACKLOG.md` (+ `_SESSION_LOG.md` doc). Suggested message: `FR-25 calc polish + data corrections (Smite split, Spellstrike, base fixes)`. **Live-verify (Chrome):** Tan/Xan show a Spellstrike toggle + a separate "Smite free enhancement" toggle beside the per-SP Smite stepper; Bonan has no Erupting line and Damage enhancement caps at 2; Minimus base reads 3; Runt base reads 2.

---

## Bugs

**BUG-1. Xanwyn Elven fluency.** Sheet shows Elven and Fey each at 1 dot (Limited); the ledger records Elven as Fluent (cost 2). Fix `xanwyn.yaml`: Elven -> Limited, cost 1. Then Xanwyn spends 2 LP = the 2 free, balanced, no conversion, and the spurious under-spent flag clears. Data-only, same-total error, which is why no budget check caught it.

**BUG-2. Granted languages not free.** Runt is Warlock / Eldritch, and Eldritch grants Fluent Deep Speech for free (classes.md l.3432). The ledger records Deep Speech as a bought Limited language (cost 1). That phantom 1 LP forces a TP->LP conversion, which is the real cause of Runt's "Trade points over-spent" and the "Language under-spent" noise. Fix: add a language grant (Deep Speech, Fluent) to Eldritch in `catalog/warlock.yaml`; teach the engine to treat granted languages as 0 cost; fix `runt.yaml` (Deep Speech Fluent, granted, cost 0). This retires the long-standing whitelisted "Runt trade over-spend" (ROADMAP / 09) as actually fixed, not suppressed. RAW-confirmed, not blocked. Overlaps FR-8.

**BUG-3. Point-budget messaging.** The "under-spent" verdicts are mostly not errors: conversions happen in whole 2-for-1 points, so an odd requirement leaves a legal spare (Xanwyn's 1 spare TP after converting 2 SP for a 3-TP need). But the wording reads like a fault, and the language line is asymmetric (only ever prints UNDER-SPENT or nothing, never balanced/over). Make the language verdict symmetric and clearly distinguish "you have a legal spare point" from "illegal / over-spent". Conversion rates themselves are correct (1 SP -> 2 TP, 1 TP -> 2 LP, character-creation.md l.90/97).

**BUG-4. Sheet overlay not mobile-responsive.** On a phone the character-sheet overlay's fixed 3-column grid (`.sh-cols` 196px+214px+1fr inside a 794px `.sh-paper`, builder_build.py l.1430) overflows and the columns overlap; the 55%-opaque backdrop lets the builder page show through behind. The `@media (max-width:640px)` block (l.1403) only restyles the builder UI, not the sheet. Fix: add narrow-width rules to stack `.sh-cols` to one column and let `.sh-paper` size fluidly, same pattern the builder already uses. Verify on a real iPhone (Phil hit this).

**BUG-5. L3 attribute picker casing.** The attribute-choice picker lists options in lower case (might, agility, ...) because that is how they are stored in the ledger. Capitalise them for display only.

**BUG-6. Runt armour / defences / DR.** Runt's armour is confirmed (magic-item audit): magical Ancestral Dwarf Armor = Defensive Heavy (+1 PD, +1 AD, PDR Half) with Rigid and Bulky stripped (the 2 Magical Power cost), so no Speed -1 and no DisADV Agility. The ledger currently mis-models it as "Deflecting Heavy +2 PD / +0 AD" with Speed/Agility penalties. Compensating errors keep PD correct (old: armour +2 / staff +0; correct: armour +1 / staff Guard +1; both sum to +2, so unbuffed PD stays 15, sheet 17 = 15 + Primal Hide daily buff). Fix set: correct the armour entry (pd 1 / ad 1 / pdr half, drop the Deflecting/Speed/DisADV note); model the wielded Quarterstaff's Guard +1 PD; apply Pact Armor's RAW +1 AD and MDR (catalog marks it "contextual, not a grant", so it is currently unmodelled, warlock.yaml l.90); populate DR = PDR Half + MDR Half, EDR none. Then run the engine to verify. Not blocked. Surfaces BUG-7.

**BUG-7. Runt AD 12 vs 14.** CLOSED 2026-07-16 (confirmed with Phil). The BUG-6 armour reading was wrong twice over: (a) the magic Ancestral Dwarven armour is **Deflecting Heavy (+2 PD, +0 AD)**, not Defensive Heavy (+1/+1) - Darryl's mistake, not Phil's; (b) Pact Armor's +1 is **AD, not PD** - the paper sheet had it in the PD box. Net: base 13 PD / 12 AD, + armour Deflecting +2 PD, + staff Guard +1 PD, + Pact Armor +1 AD (carried on the worn armour item, contextual per the catalog) = **PD 16 (18 with Primal Hide), AD 13** - which now matches the sheet, so the engine derives it clean (no more red MISMATCH). Also supersedes the old PD 15-vs-17 reading (PD was 16 unbuffed all along).

---

**BUG-8. Xanwyn missing History trade.** Xanwyn's ledger has no History trade, but his paper sheet has History at Novice (confirmed by Darryl 2026-07-16). This is why he carries the leftover 1 spare trade point that BUG-3 surfaces: the spare is really an unrecorded History (Novice) purchase. Fix: add History (Novice) to `xanwyn.yaml` trades masteries and to `knowledge_trades` (History is a Knowledge Trade). Expected effect: spent TP goes 7 -> 8 = balanced, so his "1 SPARE (legal)" advisory clears. Heads-up for execution: `builder_verify.py` section (12) currently uses Xanwyn as the SPARE example ("xanwyn spare TP reads SPARE ... in advisories, not a problem") - once History lands, move that example to another char that still has a legal spare (e.g. minimus's 6 spare skill points). Data-only, quick; a good candidate to bundle with CH-1 or a Wave 3 data pass. (Darryl offered the original sheet scan / a screenshot if we want to double-check other Xanwyn values while we are in there - ties into CH-1.)

**BUG-9. Character-sheet button placement.** The "Character sheet" button (`#sheetbtn`) sits inline in the header after the "or load a YAML" file input (h1 + badge + charsel + file input + button, plain inline flow), so on a narrower window it wraps down and left instead of staying top-right. Fix: make the header a flex row and align the button to the right (wrapping sensibly on mobile). Confirmed desired = pinned top-right (Darryl 2026-07-16). Cosmetic, easy, builder-only (`builder_build.py` header HTML + CSS).

**BUG-11. Chained point conversion mis-read as over-spent.** Found 2026-07-18 by the CH-1 minimus skill fill; fixed same day. `build_engine.py` inferred the SP->TP conversion from the trade-mastery deficit alone, BEFORE adding the TP->LP conversion, so a build whose TP deficit came from funding languages (a chained SP->TP->LP conversion) wrongly read "Trade points over-spent" despite spare SP. Minimus was the only such build and was accidentally balanced at L4 (mastery deficit 1 happened to equal the need); advancing him to L5 (earned TP 5 = mastery spend, deficit 0, but +1 TP still converted to LP) exposed it. Fix: compute the LP-funding conversion first and include it in the TP deficit (`tp_deficit = spent_tp + conv_tp - earned_tp`). Only minimus carries a TP->LP conversion, so no other ledger's verdicts move; catalog_verify stays 90/90.

**BUG-10. Picker labels show the raw grants dict.** The pact-boon and discipline picker options build their label as `name + ' %s' % grants`, so the L4 Talent (Pact Boon) picker shows e.g. `Pact Weapon {'maneuvers': 2}` (Darryl saw it as the "(maneuvers)" noise). Fix: format the grant nicely (e.g. "+2 maneuvers") or drop it from the label. `builder_build.py` `_options_for` (pact_boon + discipline branches, ~l.572/576). Cosmetic, easy.

**BUG-12. Runt's granted Psychic spell shows "Spell_Tagged" + is missing from the sheet.** On the builder page Runt's Eldritch grant (Tendrils from Beyond, the FR-8 slice-5 tag-constrained child) is labelled "Spell_Tagged" instead of "Spell" (the `spell_tagged` child-slot kind leaks into the display label), and Darryl notes it does not appear in the spell list on Runt's character sheet. Two facets: (a) cosmetic, the label should read "Spell"; (b) likely real and higher-value, `sheet()` enumerates spells from the decisions but probably does not pull `granted_spells` off the parent, so the granted spell is dropped from the sheet's spell list. Investigate both. Builder-only; not blocked.

**BUG-13. Dice roller drop-N highlights only the first kept die.** Rolling "4d6 drop 1" totals correctly but the keep/drop highlight marks only the first kept die, not all of the kept dice. Fix the highlight loop to mark every kept die. Companion, dice tab.

**BUG-14. Rules search results not ordered by section.** Search results return in an apparently arbitrary order. Sort them by rulebook section (document order) so related hits group sensibly. Companion, rules browser. **DONE 2026-07-18 (Wave B pass 1, PUSHED + CHROME-VERIFIED):** `doSearch` still selects the top 30 by relevance score, but the rendered list is then sorted by `RULES_DATA` index (document order) so same-area hits group; `_results` is left in score order so the `searchFor` fallback (`_results[0]`) still opens the best match.

**BUG-15. Condition pills lack a hover cursor.** The condition pills on the character sheet are clickable but do not switch to a pointer cursor on hover the way the other clickable elements do. Add `cursor:pointer`. Companion, char sheet. Trivial.

**BUG-16. Maneuver/spell x-delete created a silent, unrecoverable slot shortfall.** Raised by Darryl at Wave B pass 2 live-verify: Runt's L2/L4 maneuver rows carried a red x (they had an "Added in builder (reconciled per-level slots)" provenance note, so the `removable` flag fired), but those are REQUIRED budget slots, not throwaway. Deleting one dropped Runt below his granted maneuver count with no way back: there is no "+ maneuver" control, and re-picking Path (Martial<->Spellcaster) or Pact Boon (Pact Armor<->Pact Spell) only rebuilds path-rider / grant-child slots, not flat-pool maneuvers. Worse, the engine does NOT notice: it derives "Maneuvers known" as a budget (`cumulative + martial + sum_grants`) checked against the ledger's static `expected`, never by counting flat entries, so a deleted entry leaves the derived numbers unchanged and the shortfall is silent. **Fix shipped (the safety half):** maneuver/spell rows are now **edit-only** (the `removable` clause for them is dropped, so no x); you change a slot via its dropdown, you cannot delete a required slot into a broken state. Ancestry-trait removability is deliberately unchanged - ancestry DOES self-heal (the FR-9 auto ready-slot + the "+ ancestry trait" button re-add against the point budget), so removing one is never a dead-end. Harness section (23). **Auto-heal half DEFERRED (Darryl chose the full self-healing fix, but a reliable auto-fill is not feasible on the current data model - see finding):** the maneuver/spell model is a flat pool + grants, and grants are represented inconsistently - granted MANEUVERS are stored as flat entries (Runt's Cleave/Pathcarver/Brace/Side Step count in both the flat pool and `granted_maneuvers`), while granted SPELLS are NOT flat (they live in `granted_spells` or are unitemised). Measured entry-count vs the derived budget: maneuvers line up for all six, but spells are "short" by the grant amount on four PCs (bonan 1/3, runt 5/6, scaletrix 6/10, xanwyn 4/5). So a naive "auto-fill empty slots up to the budget" would show PHANTOM spell slots on those PCs. A correct auto-heal needs the deferred maneuver/spell data-model unification (the FR-8 surgical boundary / FR-12-13 territory). With deletion removed, a shortfall can no longer be created through the UI, so the edit-only fix already delivers the safe, consistent end state; the true auto-heal is logged as a follow-on tied to the data-model work. **Confirm with Darryl:** is edit-only sufficient (recommended), or pursue the data-model-dependent auto-heal.

## Features

**FR-1. Sort character picker alphabetically.** The Companion party chips already sort; the builder's picker may not. Minor.

**FR-2. Finish DR.** The engine's DR mechanism is done and plumbed (collects `pdr`/`edr`/`mdr` off equipment, value int or "half"). Runt's values are now known and handled in BUG-6. Remaining: work out the other PCs' DR (most likely none for the non-heavy-armour PCs) and show the rules reference alongside each DR entry.

**FR-3. Level-up plans for all PCs.** Extend Tan's L5/L6 plan support to the others. Needs: an "Add Planned Level" button (and Undo must not vanish when used), and the ledger's `skills` section folded into the per-level blocks rather than sitting separately, so planned levels can carry their own skill picks. (Merges the two level-up-plan entries from Darryl's list.)

**Decomposition (planned 2026-07-17).** Two slices. **Slice 1 (builder-only, no ledger reshape):** an "Add Planned Level" button that works for every PC + editable plans + undo. **Slice 2 (the structural half):** fold the flat `skills.masteries` aggregate into per-level blocks so planned levels carry their own skill picks. Darryl's call (2026-07-17): Slice 2 uses the **Hybrid** migration - keep the aggregate as the source of truth for existing/inferred skill history, add per-level skill blocks only for planned levels going forward (lowest risk of shifting the verified totals / breaking 90/90).

**Slice 1 SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-17 (origin head `b07a291 "FR-3 slice 1"`, on `ae74e48` FR-6).** Darryl committed + pushed and confirmed the live Chrome check - added a planned Level 5 on Bonan and selected items in it, and Tanrielle still shows her L5/L6 (read-only). Every character can now build a multi-level plan the way Tanrielle's L5/L6 were hand-authored. **Root cause of the old "Tan-only" limit:** there was no add-a-plan action at all - the single level button (`add_level`) always ADVANCES `current_level` (generate-or-promote), so a *planned* (future, non-current) level block could only ever be hand-written into the YAML; Tanrielle just happened to have hers written in. **What shipped (builder-only, all in `builder_build.py`):** a new `add_planned_level()` API appends the next level ABOVE the highest existing level (completed / current / already-planned, capped at L10) as a dashed plan group WITHOUT advancing `current_level`; the spine-slot generation is factored out of `add_level` into a shared `_gen_level_slots(new, plan=False)`. **Editable plans (Darryl's call):** a builder-generated plan is fillable - each generated slot carries a `plan_edit` flag and `_dec` renders those plan rows as real editable pickers (attribute / talent / path / subclass / spell / maneuver), while a hand-authored locked plan (Tanrielle) has no flag and stays a read-only preview. The flag round-trips through export/reload. **Undo hardened:** the undo snapshot now records the exact `added` level and whether it was an `advanced` (real level-up) vs an appended plan, so undo restores correctly for both and the undo link is always labelled with the real level and never wrongly vanishes; `state()` exposes `can_plan` / `plan_level` for the new button and drives `undo_level` off `added`. Plans are speculative: the engine replays only to `current_level`, so a plan raises NO problems and does not touch derived stats/budgets. **No engine/catalog/ledger/Companion change** (no party YAML edited; `API_PY` still baked, so section-1 `api blob == API_PY` holds and catalog_verify stays 90/90 by construction). Verified in a fresh clone at head `ae74e48`: catalog_verify **90/90 zero deltas**; builder_verify PASS all sections incl. NEW section **(18)** (21 checks: minimus with no plan gets the button; add_planned_level appends L5 without advancing; the undo link labels the real level; the plan rows are editable pickers; a plan raises no problems and stays engine-clean; editing a plan pick writes and stays editable; a second plan stacks and undo unwinds L6 then L5; plan_edit round-trips export/reload; Tanrielle's locked L5/L6 stay read-only while she can still stack a plan at L7; a plan can be promoted by Add level and undo restores it as a plan; planning stops at the L10 ceiling); inline JS `node --check` OK; `builder.html` regenerated (2,434,083 bytes). Files: `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`. Live-verify via Chrome after push: open the builder for any PC (e.g. `?char=minimus`), click "Add planned level L5" - a dashed plan group with editable pickers appears without changing the current level, "undo add L5" stays until clicked, and a second click stacks L6. **Next: Slice 2 (Hybrid skills-fold).**

**Slice 2 SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-17 (origin head `6c02db2 "FR-3 slice 2"`, on `b07a291`).** Darryl committed + pushed and confirmed the live Chrome check. Planned levels now carry their own skill picks, reusing the FR-8 grant-child backbone. **Known follow-up (raised by Darryl on live-verify, now FR-17):** the plan picker does normal within-cap raises (e.g. Runt L5 Intimidation Novice->Adept) but does NOT offer the "spend 1 skill point to raise the Mastery Limit by 1" purchase (`limit_raise: skill_point_purchase`, the flat allocator's "cap+"), so it cannot push a skill ABOVE the normal level cap the way Tanrielle's hand-authored L5 does (Awareness Adept->Expert via a carried limit purchase). See FR-17. **Design calls confirmed with Darryl before code:** reuse the FR-8 child-slot backbone (over a bespoke per-level skills block); **ENFORCE the skill budget on the plan** (over the capture-only/speculative option - this deliberately diverges from Slice 1's "a plan raises no problems", scoped to skills); a pick can both add-new and raise-a-tier. **Hybrid, no ledger reshape:** the flat `skills.masteries` aggregate stays the source of truth for existing/inferred history (untouched on all six ledgers), and ONLY builder-generated planned levels gain structured, editable skill picks - so the six baselines are byte-identical at rest and catalog_verify stays 90/90 by construction. **What shipped (all in `builder_build.py`):** `skills` joins `GRANT_CHILD_SLOTS` (-> singular `skill`); `add_planned_level`/`_gen_level_slots` emit ONE per-plan-level carrier entry `{slot:'skills', grants:{skills:N}}` (N = the class-spine `skill_points`) whose grant materialises N editable skill child-slots via the existing backbone (a real advance via `add_level` emits NO carrier, so completed/current levels keep the flat aggregate - the surgical Hybrid boundary). Each skill child pick is stored as `"<Skill>: <resulting tier>"` (a colon does not trip `is_composite`, so it round-trips through export/reload). `_grant_children` special-cases skills to give each slot **level-aware options** (`_skill_plan_options`: the next tier up from a running state = flat aggregate + raises from lower plan levels, only within `_mastery_cap_idx(level)` = the DC20 Mastery Limit) + **sibling distinctness** (a skill picked in one slot of a level is hidden from the others). **Enforcement:** `builder_problems` flags each undecided planned skill (under-spend; over-spend is impossible since N slots = N points), and `catalog_problems` defends against stale/hand-authored illegal values (unknown skill, not a single step up, over the level cap, duplicate in one level) - the picker itself only ever offers legal picks (FR-13-style). Tanrielle's hand-authored L5/L6 `slot: skill` rows stay read-only (locked plan, not builder-generated). No JS change (the generic decision renderer already renders any editable child picker; the carrier renders as a fixed "N skill points" header). Verified in a fresh clone at head `b07a291`: catalog_verify **90/90 zero deltas**; builder_verify PASS all sections incl. NEW section **(19)** (baselines clean at rest with no carrier; L5 budget materialises a carrier + 2 editable skill slots keyed GC#L5:...#skills#k; both undecided picks flagged as enforced; add-new offers Novice; sibling distinctness; filling clears problems; raise + mastery cap on Tanrielle - Adept can't raise at L8 but can at L10; running state chains across plan levels; Tanrielle's locked rows stay read-only; granted_skills round-trips export/reload; a stale 2-step/over-cap value is flagged; a REAL advance creates no carrier) and section (18)'s plan-contract assertions updated for the skills divergence (a plan is engine/catalog clean and raises only the enforced skill problems); section (13) `GRANT_CHILD_SLOTS` boundary updated to include skills (spells/maneuvers still excluded); inline JS `node --check` OK; `builder.html` regenerated (2,437,345 bytes). Files: `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html` (NO ledger/catalog/Companion change). Live-verify via Chrome after push: open the builder for any PC (e.g. `?char=minimus`), add a planned level that grants skill points (e.g. L5) - a "N skill points" row with N editable skill pickers appears under the plan; each picker offers add-new (Novice) and legal raises within the level cap, hides a sibling's pick, and shows undecided until filled; a real "Add level" advance instead has no such row. **With Slice 2, FR-3 is complete and the Wave 3 remainder is clear.**

**FR-4. Rename a char / file.** Needs a scoping decision first. Display-name rename is trivial (builder already separates `character` from the stable `handle`). Handle/file rename is the coordinated one: the id appears in the companion-key -> builder-handle map, the yaml filename, PARTY_LEDGERS/CHARS, localStorage keys, and `?char=` deep links. Decide display-only vs handle/file vs both.

**Display-name slice DONE 2026-07-18 (Darryl's call: pull the trivial display-only part out, leave handle/file parked; PUSHED + LIVE-VERIFIED origin `7676679`).** The `set_meta('character', ...)` backend already existed and an editable name field was already wired, but gated to new-from-scratch only (`if(s.scratch)`), so the six canon PCs had no rename control. Change (builder-only, `tools/builder_build.py`): the metacard's canon branch (previously just `$('metacard').style.display = "none"`) now renders a slim, name-only rename card wired to `set_meta('character', ...)` through `refresh()`, so a rename marks the build dirty and raises the existing "EDITING CANON" warning bar (nothing saves until export). Deliberately NOT the full meta card (ancestry pickers stay scratch-only). A scope note under the field states the handle and `?char=` deep link are unchanged and the export filename slug follows the new display name (`slug()` already derives from `character`). No engine/catalog/ledger/Companion change, so catalog_verify stays 90/90 by construction. Harness section **(26)**. **The handle/file rename half stays parked** (still needs the display-only-vs-handle/file-vs-both scope call for the coordinated id rename across the companion-key map / yaml filename / CHARS / localStorage / deeplinks).

**FR-5. Unsaved-changes guard.** If edits are unsaved and a different character is picked from the dropdown, warn that changes will be discarded and offer to save/export first.

**FR-6. Rules text for a chosen option.** Some way to see the rule for a chosen trait/ability/spell (link to, or hover over). The Companion already auto-links bold rules terms (linkifyTerms); reuse that idea in the builder. DONE 2026-07-17 (pushed + live-verified) - baked the shared rules corpus into the builder + ported linkifyTerms + a click-open rule panel; see the FR-6 SHIPPED note above.

**FR-7. Refilter dropdowns.** Remove already-chosen items from a dropdown's options so the same one cannot be picked twice.

**FR-8. Choice-bearing talents/subclasses need pickers (epic).** Talents and subclasses that grant a set of choices should present real slots/pickers, with the granted items flowing from the choice rather than being baked into a name. Examples: Rune Knight subclass (choose 2 runes); Sorcerer L4 talent (choose 2 metamagic); Sorcerer L2 talent (choose 2 spells); Warlock Eldritch subclass (grants Deep Speech + a Psychic-only spell); Martial Expansion (maneuver slots); Pact Armor / Pact Weapon (grant Defensive / Offensive maneuvers, currently named in text, should be removed from the name and granted). Pact Weapon/Armor is partly done (granted_maneuvers captured structurally); the remove-from-name step and the general pattern remain. Overlaps BUG-2 (Eldritch language grant). Large: treat as an epic and split when we plan it.

Folded in here (Darryl 2026-07-16): **the L1 Pact Weapon is not a picker** - at L1 the pact-boon renders as fixed text, unlike the L4 Pact Armor which IS a picker. Root cause: the L1 pick string bundles the boon name + the weapon + its comma-separated properties (`Pact Weapon: Staff (Powerful focus; 1B, 2H, Guard, Reach, Impact)`), and the comma trips `is_composite()`, so the row falls back to text. The fix is the same "separate the boon name from the bundled description/grants" step this epic already calls for: make the L1 pact-boon a clean picker (boon name only; keep the staff + its properties as structured/equipment data or a note), matching L4. Do it as part of FR-8 rather than a one-off.

**Decomposition (planned 2026-07-17).** FR-8 splits into five shippable slices: (1) L1 Pact Weapon clean picker (self-contained; DONE 2026-07-17); (2) generalise "grants -> child slots" into a reusable backbone (structured grants auto-materialise typed picker-slots, linked to the parent so re-picking rebuilds them); (3) choice-bearing subclass Rune Knight (choose 2 runes; target Xanwyn Flame+Water; needs a rune catalog + a subclass sub-choice pattern); (4) choice-bearing talents (metamagic choose 2, spells choose 2, Martial Expansion maneuver slots; reuses slice 2 + existing spell/maneuver slot machinery); (5) Eldritch constrained spell grant (Psychic-only pick; meets FR-13 legality filtering, naturally last). Slice 1 is independent; slices 3/4/5 lean on slice 2.

**Slice 1 SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-17 (head `6d67c2a "FR-8 slice 1"`, on `dc4f762`; Darryl confirmed).** Runt's L1 Pact Weapon now renders as a clean, editable picker exactly like the L4 Pact Armor. **The real root cause was deeper than the comma-laden string:** Runt's chargen `class_choices` wrapper used `slot: pact_boon` (singular), but `_decisions` only treated the plural `pact_boons` (what `blank_ledger` creates for a new warlock) as a picker wrapper, so the entry fell through to the fixed-text `else` branch (id=None) and never became a picker. The composite string was a second, cosmetic problem on top. Fix: (a) `builder_build.py` `_decisions` opt_slot now accepts `slot` in `('pact_boons', 'pact_boon')` so either wrapper spelling renders as a picker (grant COUNTS were already slot-name-agnostic via `_total_granted`, so no accounting change); (b) `runt.yaml` de-bundled the pick from `Pact Weapon: Staff (...)` to a clean `Pact Weapon`, folding the staff/properties into the boon note (the Staff of Lightning is already an equipment entry, so nothing lost). `set_decision`'s existing `cg:choice` branch already handles editing the L1 boon (re-aggregates grants across picks). New `builder_verify` checks lock it in: the L1 boon is a picker (current Pact Weapon, 4 options) and re-picking re-aggregates grants. catalog_verify 90/90 zero deltas, builder_verify PASS. Files: `builds/runt.yaml`, `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`. Companion unaffected (Runt's derived numbers unchanged). Known Slice-2 gap noted: the `cg:choice` edit path does not yet clear a parent choice's stale `granted_maneuvers`/`granted_spells` on re-pick (the L4 level path does); harmless for canon, folds into slice 2.

**Slice 2 SHIPPED + PUSHED 2026-07-17.** The reusable "grants -> typed child picker-slots" backbone. A grant-bearing parent (boon / discipline / talent / subclass) that carries a pickable grant now auto-materialises typed child picker-slots, keyed structurally to the parent: the picks live in a `granted_<resource>` list ON the parent, the child decision id encodes the parent (`GC#<parentref>#<resource>#<k>`, where parentref is `cg:<ci>` or `L<lvl>:<idx>`), so re-picking or removing the parent rebuilds/drops them with no separate back-reference and no orphans. New `builder_build.py` glue: `GRANT_CHILD_SLOTS` (the pickable resources `{runes, metamagic}`, extensible), `_grant_children` (rendering, called after every grant-bearing parent in `_decisions`), `_apply_grants` (the single rebuild helper: sets/pops `grants`, clears stale maneuver/spell provenance on a real option change, resizes each `granted_<resource>` to the new count), `_set_grant_child` (routes `GC#` ids in `set_decision` into the parent's list); `_options_for` gains rune/metamagic branches (empty until the catalogs land); `builder_problems` counts undecided child slots against the grant count. **Surgical boundary (Darryl's call):** maneuvers/spells are deliberately left on the existing flat-pool + `expand_composite` model, NOT rendered as child slots, so no canon migration and the baseline is byte-identical at rest (Runt has zero `GC#` rows). **The known slice-1 gap is closed:** `_apply_grants` is now wired into the discipline, talent, level `pact_boon` AND the chargen `cg:choice` branches, so re-picking a chargen boon clears the old boon's stale `granted_maneuvers`/`granted_spells` (symmetric with the level path). Verified: catalog_verify 90/90 zero deltas; builder_verify PASS incl. new section **(13)** (synthetic rune-grant fixture: a level parent materialises 2 typed rune child pickers keyed to it; editing a child writes into `granted_runes`; re-picking a boon to a rune-granting option rebuilds the children and clears old `granted_maneuvers`; re-picking to a non-rune boon drops both; the closed chargen clear-gap; the surgical boundary); inline JS `node --check` OK; `builder.html` regenerated (280,564 bytes). Files: `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`. No ledger or catalog change (so the Companion is unaffected). This backbone unblocks slices 3 (Rune Knight runes: add a rune catalog + wire the subclass grant), 4 (metamagic / Martial Expansion talents), 5 (Eldritch constrained Psychic spell).

**Slice 3 SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-17.** The Rune Knight subclass is now a choice-bearing parent that grants 2 runes, materialising 2 rune child picker-slots via the slice-2 backbone, targeting Xanwyn (Flame + Water). Three parts: **(1) rune catalog** - `catalog_build.py` gains a curated `RUNES` (the 6 short names Earth/Flame/Frost/Lightning/Water/Wind, classes.md l.3081-3116; no resource grants, their effects are passive / on-Smite riders) merged into the Spellblade extras, so `spellblade.yaml` now carries a `runes:` list (same shape as disciplines/pact_boons) and `_options_for('rune')` returns the 6; **(2) subclass grant** - `SUBCLASS_GRANTS["Spellblade"] = {"Rune Knight": {"grants": {"runes": 2}}}` populates `spellblade.yaml`'s `subclass_grants` side-map (the existing mechanism, already used for Warlock Eldritch and read by `_grant_tags`; chosen over turning `subclasses:` into dicts to avoid touching parse_subclasses / the 3-subclass assertion), and the subclass branch of `set_decision` in `builder_build.py` - **the one grant-bearing branch slice 2 left un-wired** - now looks up `subclass_grants[value]` and routes through `_apply_grants`, so re-picking the subclass rebuilds/clears its rune slots (symmetric with the discipline / pact_boon / talent branches); **(3) Xanwyn ledger** - his L3 subclass entry is de-bundled from the composite name `"Rune Knight (Flame + Water)"` (which tripped `is_composite` via the " + ", so it rendered as FIXED text with a replace hatch, exactly the slice-1 Pact Weapon situation) to a clean `pick: "Rune Knight"` + `grants: {runes: 2}` + `granted_runes: [Flame, Water]`, and the top-level `subclass:` field to `"Rune Knight"`. Now his subclass row is a real editable picker with 2 rune child-pickers reading Flame + Water under it. His `expected:` block is unchanged (runes carry no resource deltas), so the derived oracle holds. Verified: catalog_verify **90/90 zero deltas** (new checks: the 6 runes present + Rune Knight grants runes:2; Xanwyn's granted_runes all valid catalog runes; the existing subclass-grant check validates `grants == {runes:2}`); builder_verify PASS incl. new section **(14)** (the real Spellblade rune catalog populates the 'rune' picker; Xanwyn's subclass is a clean editable picker not fixed text; the {runes:2} grant materialises 2 rune child-pickers keyed to the subclass reading Flame + Water with 6 options each; both decided so no completeness problem and the build stays clean; re-picking to Paladin drops the grant + children, re-picking Rune Knight rebuilds 2 undecided slots, editing a child writes into granted_runes); inline JS `node --check` OK; `builder.html` regenerated (281,888 bytes). Files: `tools/catalog_build.py`, `tools/builder_build.py`, `tools/catalog_verify.py`, `tools/builder_verify.py`, `builds/xanwyn.yaml`, regenerated `builds/catalog/spellblade.yaml` + `builds/builder.html`. **Companion unaffected** (Xanwyn's derived numbers unchanged; the Companion already displays a clean "Spellblade (Rune Knight)" and a correct Flame/Water Rune Weapon card). Next: slice 4 (metamagic choose-2 / spells choose-2 / Martial Expansion talents), then slice 5 (Eldritch constrained Psychic spell).

**Slice 4 SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-17.** Meta Magic is now a choice-bearing talent that grants 2 metamagic options, materialising 2 metamagic child picker-slots via the slice-2 backbone, targeting Scaletrix (Quickened Spell + Vicious Spell). **Key correction to the prior handoff:** it claimed no party member had the Meta Magic talent, so metamagic looked like a synthetic-fixture-or-nothing case. Not so - `scaletrix.yaml` L4 had a real pick `"Meta Magic (Quickened Spell, Vicious Spell)"` (she took BOTH Innate Power at L2 and Meta Magic at L4), with the two enhancements bundled into a composite name (the comma tripped `is_composite`, so it rendered as fixed text with a replace hatch - the slice-1 Pact Weapon / slice-3 Rune Knight situation again). So metamagic is a real canon target, and slice 4 is a near-exact clone of slice 3 - and *cheaper*: the talent branch of `set_decision` was ALREADY wired through `_apply_grants` (slice 2), and `metamagic` was ALREADY in `GRANT_CHILD_SLOTS`, so no new runtime wiring at all. Three parts: **(1) cat-level metamagic catalog** - a NEW hand-curated cross-cutting list `builds/catalog/metamagic.yaml` (6 options Careful/Distant/Quickened/Subtle/Transmuted/Vicious Spell, classes.md l.2618-2639; no resource grants - each is a per-use 1 MP Spell Enhancement in play, not a build resource). **Darryl's call: cat-level not class-scoped** (unlike runes, which live in `spellblade.yaml`) because Meta Magic is reached via a cross-class MC-Sorcerer feature, so any character multiclassing into Sorcerer reads the same list; `metamagic` added to `builder_build.py` `CATALOG` + the baked `CATPATHS` map, and `_options_for('metamagic')` reads `self.cat['metamagic']['options']` (the rune/metamagic branch of `_options_for` was split: rune stays on `ccat`, metamagic reads cat-level). **Full option names** (Darryl's call), e.g. "Quickened Spell", matching the ledger and rules. **(2) talent grant** - `grants: {metamagic: 2}` added to the Meta Magic `mc_feature` in `talents.yaml`; because the talent branch already routes grants through `_apply_grants`, re-picking rebuilds/clears the metamagic child-slots with no code change. **(3) Scaletrix ledger** - her L4 talent de-bundled from the composite name to `pick: "Meta Magic"` + `grants: {metamagic: 2}` + `granted_metamagic: [Quickened Spell, Vicious Spell]`; her `expected:` block is unchanged (metamagic carries no resource delta), so the derived oracle holds. Now her L4 talent row is a real editable picker with 2 metamagic child-pickers reading Quickened Spell + Vicious Spell. Verified: catalog_verify **90/90 zero deltas** (new checks: the 6 metamagic options present in classes.md + Meta Magic grants metamagic:2; Scaletrix's granted_metamagic all valid catalog options + count matches the grant); builder_verify PASS incl. new section **(15)** (the cat-level metamagic catalog populates the 'metamagic' picker; Scaletrix's talent is a clean editable picker not fixed text; the {metamagic:2} grant materialises 2 metamagic child-pickers keyed to the talent reading Quickened/Vicious with 6 options each; both decided so no completeness problem and the build stays clean; re-picking to Life Tap drops the grant + children, re-picking Meta Magic rebuilds 2 undecided slots, editing a child writes into granted_metamagic); the baked `catalog blob metamagic` decodes to source; inline JS `node --check` OK; `builder.html` regenerated (284,564 bytes). Files: NEW `builds/catalog/metamagic.yaml`, `builds/catalog/talents.yaml`, `tools/builder_build.py`, `tools/catalog_verify.py`, `tools/builder_verify.py`, `builds/scaletrix.yaml`, regenerated `builds/builder.html`. **Companion unaffected** (Scaletrix's derived numbers unchanged). Next: slice 5 (Eldritch constrained Psychic-spell grant, meets FR-13).

**Slice 5 SHIPPED + PUSHED + LIVE-VERIFIED 2026-07-17 (the LAST FR-8 slice - FR-8 EPIC COMPLETE).** The Warlock Eldritch subclass's Otherworldly Gift grant (learn 1 Spell with the Psychic Tag) is now a real, named, Psychic-CONSTRAINED child picker on Runt, defaulting to Tendrils from Beyond - the phantom +1 (previously only a note, unnamed, just a budget bump) is now itemised. Approach = the (a-narrow) constrained child-slot Darryl confirmed: a targeted, opt-in crossing of the slice-2 surgical spell boundary, NOT the party-wide migration. Design calls confirmed with Darryl: (a-narrow) child-slot over the flat-pool legality filter; build_engine.py stays budgets-only (live legality already lives in the builder's catalog_problems, which the builder runs). Five parts, all in the API_PY glue in `builder_build.py` plus the ledger/harness: **(1)** `_spell_grant_tag(parent)` gates the new behaviour - it returns a tag ('Psychic') ONLY for a subclass whose catalog `subclass_grants[...]` carries a `spell_access` tag AND grants spells; plain `spells` is deliberately NOT added to `GRANT_CHILD_SLOTS`, so the other five `{spells:2}` grants (bonan/scaletrix/tanrielle/xanwyn/plus Runt's flat picks) stay on the flat-pool model. **(2)** `_grant_children` gains a second, gated pass that materialises ONE `spell_tagged` child for a tag-constrained spell grant; the child id reuses resource `spells` (`GC#<parentref>#spells#<k>`) so `_set_grant_child` writes `granted_spells` unchanged. **(3)** `_spell_tagged_options` = accessible spells (`_spell_access`) intersected with the character's granted tag (`_grant_tags`), so the picker offers exactly the Psychic-tag spells (Tendrils is legal via the tag though its Conjuration school is not chosen); the constraint is sourced entirely from the EXISTING `subclass_grants.Eldritch.spell_access.tag` side-map + `spell_meta` tags, no new catalog data. **(4)** `_apply_grants` resizes `granted_spells` for a tag-constrained entry (so re-picking the subclass rebuilds/clears the slot, symmetric with runes/metamagic); `builder_problems` counts the undecided Psychic slot; `_chosen_names('spell')` now includes `granted_spells` so the flat spell pickers hide the granted spell (no double-pick). **Consume-not-stack:** the `{spells:1}` grant is already counted by the spell budget via grant totals (exactly like `granted_maneuvers`), so itemising it in `granted_spells` adds nothing - build_engine derives **Spells known 6/6 OK**, `expected.spells` unchanged. **(5)** `runt.yaml` L3 Eldritch gains `granted_spells: [Tendrils from Beyond]` + a rewritten note. **FR-13:** the builder now offers ONLY legal (Psychic-tag) options for this grant and cannot make an illegal pick through the UI; `catalog_verify` now also validates granted spells offline (it prints "spell Tendrils from Beyond legal via tag {'Psychic'}"), so the constrained grant is legality-checked both live and offline - the FR-13 tag story is delivered for this grant without touching build_engine. Verified: catalog_verify **90/90 zero deltas** (+ the granted-spell legality sweep); builder_verify PASS incl. NEW section **(16)** (constrained picker offers only Psychic incl. Tendrils and excludes non-Psychic Lightning Bolt; exactly 1 `spell_tagged` child keyed to the subclass reading Tendrils; build stays clean; the granted spell is hidden from flat pickers; the other five `{spells:N}` grants get NO spell child; re-pick to Fey drops it, back to Eldritch rebuilds 1 undecided slot that surfaces as a problem, editing the child writes granted_spells) and the slice-2 section (13) boundary check tightened to "Runt's ONLY GC# row is the slice-5 Psychic slot, plain spells/maneuvers still not in GRANT_CHILD_SLOTS"; the baked api blob decodes to the edited source; inline JS `node --check` OK; `builder.html` regenerated (291,316 bytes). Files: `tools/builder_build.py`, `tools/catalog_verify.py`, `tools/builder_verify.py`, `builds/runt.yaml`, regenerated `builds/builder.html`. **Companion unaffected** (it does not read `granted_spells`; Runt's derived numbers, spells known = 6, are unchanged; the deploy re-bakes both apps from source). No new catalog file. On push, the FR-8 epic (all 5 slices) is complete.

**FR-9. Expand ancestry traits into slots.** Same slot/expand treatment the maneuvers got (confirmed). Note it is more involved than maneuvers: ancestry traits are budget-constrained, each has a variable cost (0 / 1 / 2, or a negative drawback that refunds points) and some have prerequisites, so the generated slots must sum to the L1 budget (5 points, +2 at later levels), not just fill a fixed count. This cost/budget machinery is exactly why ancestry_trait was excluded from the original maneuver reconcile.

Runt's data is now resolved (Darryl supplied Runt's L1 sheet, 2026-07-16). His 5 Ancestry Points itemise fully from the Giantborn (ogre) list: Powerful Build (2), Brute (1), Tough (1), Titanic Toss (1), Mighty Hurl (1), Unyielding Movement (0), Heavy Riser (-1) = 5. All seven are already in the catalog with these exact costs (ancestries.yaml Giantborn l.84 to 98), so no catalog work is needed, only a `runt.yaml` edit: replace the "L1 traits not itemised (remainder)" placeholder and drop the inferred flag on Tough, listing the seven traits explicitly. Tough's source tag is correct (it is in the Giantborn list l.86 as well as Dwarf l.54), so the earlier source-slip worry is void. This closes the ROADMAP / 09 "Runt L1 ancestry itemisation, ASK PHIL" open item. Bonus: Powerful Build / Brute / Titanic Toss / Mighty Hurl are the mechanical basis of Runt's grapple-and-throw build in file 10, now structured.

**DONE 2026-07-18 (Wave B pass 2, Option A - polish + lock, Darryl's call; AWAITING PUSH).** Key finding on execution: the slot machinery this ticket describes was **already built** incrementally (per-trait pickers with full catalog options, `+ Add trait` / per-row remove, catalog-cost auto-applied on pick incl. negative-refund drawbacks, sum-to-budget enforced by the engine), and Runt's 7 Giantborn traits were already itemised in `runt.yaml` (the Wave 1 data edit). So the remaining gap was UX parity with the skills allocator, not an engine/ledger rebuild. Darryl chose Option A over the full FR-8-backbone re-plumb (which would look identical to a player but risk the 66 stat checks). Delivered (builder-only): `_anc_budget()` -> `state.anc_spent`/`anc_budget` -> a live **"Ancestry points: N of M spent"** readout by the `+ Add trait` control; plus an **auto ready-slot** (`cg:trait:+`) that shows one empty ancestry picker while under budget and no open slot exists (materialised into a real trait on first pick via `set_decision`, chaining like the allocator; suppressed at budget and when an undecided slot already exists, which keeps the harness safe). Harness section (22). No `runt.yaml`/catalog/Companion change. See the Wave B pass 2 shipped note above.

**FR-10. Level grants in the section header.** Echo what each level grants (numeric deltas + named features, from the class spine) into that level's collapsible section header, so a collapsed group shows what it gives without expanding. Done for every level of every character (generalised 2026-07-17 from the initial cur+1-only echo, which only lit up Tanrielle's planned L5).

**FR-11. Gear catalog / picker (Tier B).** A curated list of the party's real items (weapons, armour, shields, focuses) so items are picked from a dropdown with properties auto-applied, instead of hand-typing effect fields. Pure convenience; would cut hand-entry transcription errors like the language ones. Parked for now; not required for any reconciliation (gear effects already work, see Parked note on Tier A/C).

**FR-12. Add full DC20 class + ancestry data coverage.** Extend the catalog beyond the party's six classes to all 13 (and remaining ancestries), so the party can build any character, including on mobile. Runtime/size is NOT the blocker: the builder runs the Python engine via Pyodide (a multi-megabyte WASM runtime from CDN), which dominates load and cold-start regardless of catalog size. The full catalog is roughly 100 to 150 KB of YAML (names/costs/prereqs/tags, not rule text), taking builder.html from ~262 KB to ~350 to 400 KB, invisible next to Pyodide, and the engine only ever replays the one character being built, so validation does not slow down. The real costs are (a) human: each class needs a catalog file AND a hardcoded level-progression table in `build_engine.py`, plus data entry, plus the 0.11 beta refresh treadmill; and (b) mobile UX: pickers listing hundreds of options need search/typeahead and FR-7 filtering to stay usable. "Only load classes in use" lazy-loading was considered and rejected, it would shave ~100 KB off a multi-MB load, so buys nothing for performance. Large: treat as an epic. Depends on FR-7 (and ideally a picker search box) for the mobile experience, and on FR-13 to stay trustworthy once pickers span the full catalog.

**Re-raised 2026-07-18 (Darryl):** pair full coverage with a load mitigation, framed as "only load the classes & ancestries in use" vs "add the rest of the data." The prior finding stands: the Pyodide WASM runtime dominates load and cold-start, so lazy-loading ~100 KB of catalog YAML off a multi-MB load buys little for performance. The real levers are picker UX (search / typeahead + FR-7) and keeping the engine replaying only the one character being built. Treat the load question as part of this epic, not a separate item.

**FR-13a. Constrained spell-grant sourcing (scoped 2026-07-19 from Scaletrix live-verify).** The builder's spell pickers offer the character's WHOLE accessible list, so a spell slot that should be constrained to its granting feature's source/school shows the wrong (usually L1-looking) options. Surfaced on Scaletrix, whose spells come from several different feature-specific lists: **Fiendish Magic** (ancestry trait "Arcane Spell") grants 1 Arcane spell limited to Elemental or Enchantment schools (this is where Command came from at L1, and it should be childed under the ANCESTRY trait, not sit as a class spell); **Druid** grants Primal-source spells (his 4 L1 + 1 at L3); **Spellcaster Path** grants 1 spell each time taken (L2 + L4 = 2), source unspecified; **MC Sorcerer / Innate Power** (L2) requires a **Sorcerous Origin** sub-choice that is not modelled anywhere yet, and its Intuitive Magic option grants 2 spells from the chosen Sorcerer list (Arcane / Primal / Divine - he chose Arcane). So the piece is: (1) attribute each granted spell to its granting feature and child it there (like the FR-8 slice-5 Eldritch Psychic pattern, generalised); (2) filter each such picker to that feature's source/school list; (3) model the Sorcerous Origin (and Intuitive-Magic list) choice nodes. This is squarely FR-13 (live spell legality by source/school) plus FR-8-style choice modelling, and it eventually touches every caster, not just Scaletrix. Depends on / overlaps FR-13 and the FR-12 conversation. Scaletrix's currently-unrecorded spells (the auto-heal ready slots) are the visible symptom; ASK DAMO for his actual picks is the data side. Treat as its own thread.

**FR-13. Live spell & maneuver legality.** Enforce that each chosen spell/maneuver is actually accessible to the character: a spell from one of their chosen schools/sources (plus tag grants like Eldritch's Psychic access), and a maneuver of the right type for its slot (Pact Armor's slots need Defensive maneuvers), rather than only counting them and checking they are real catalog entries. **The maneuver-type half is DONE (2026-07-19): pact-boon maneuver grant-children are now filtered to the boon's `maneuver_type` (Pact Weapon = Attack, Pact Armor = Defense), catalog-driven via `warlock.yaml`. The spell-source half is FR-13a.** Current state: the offline harness already validates this for the six known builds (catalog_verify.py section 2, per-class access models), so there is no correctness risk today. The gap is in the live engine the builder runs (build_engine.py), which does counts and budgets but no school/type legality (its docstring lists this as "not yet", l.15). Low urgency now (known, hand-verified builds), but it becomes important with FR-8 (choice pickers) and especially FR-12 (open pickers over the full catalog): the builder should then offer only legal options and flag illegal picks. Folds into the FR-8 / FR-12 picker work. Bundled minor: per-skill die bonuses (the engine derives skill bonus as attribute + flat mastery, and models no skill-specific die mechanics), low priority.

**FR-14. Recent Files + deeplink auto-add.** A "Recent Files" list at the top of the character picker, persisted in localStorage (already used for the resume/in-progress state, l.1736), auto-adding a character when the builder is opened via a Companion deeplink (`?char=<handle>`). Straightforward, self-contained, and it pairs with the level-up-night flow (each player deeplinks from the Companion, their character lands in their own recent list). Do this part regardless.

Folded-in design decision (Darryl's motivation): use it to stop showing the hard-coded party in the default dropdown, so outsiders given the builder do not see our characters. Key finding from the code: on the live site the party ledgers are base64-**baked** into builder.html and that bake is what serves (the deploy publishes only builder/index/howto html, so the fetch-first path 404s and falls back to the bake). Two levels:
- **Level A (minimal, no deploy change):** stop *listing* the party in the default dropdown but keep them baked, so deeplinks still resolve (a `?char=` lookup hits the baked blob directly, independent of the dropdown). Outsiders see an empty picker (New / Load / Recent); the party arrives via deeplink and auto-populates recent files. Declutter + soft privacy, deeplinks and robustness preserved.
- **Level B (stronger, needs a deploy change):** stop baking the party ledgers and publish the yaml to `dist/` so the page ships with no embedded party data and deeplinks fetch it. Only this actually removes the party from the public page source (today anyone with the builder URL can read the full party builds out of the baked blob). Tradeoff: exercises the fetch path in production (currently dev-only), and the yaml is still fetchable at known URLs, so it is not-embedded / not-shown, not access-controlled. Party data is table-facing (no GM secrets), so soft privacy is acceptable.

Caveat: Recent Files is a convenience layer (per-device, user-clearable, transient), so it must not become the only route to a party character. The canonical ledger must still exist and resolve (baked in Level A, published in Level B); the hard-coded *source* is not redundant, it just need not be *shown*. Recommendation: ship the Recent Files feature + Level A; treat Level B as an optional follow-on.

**FR-15. Trades show a numeric mastery bonus on the character sheet.** Today the sheet prints trades as a tier only (Novice/Adept/...) with no number, because DC20 trades have no fixed governing attribute (skills do; the sheet computes skill bonus = governing attribute + mastery). Decision (Darryl 2026-07-16): show the **mastery bonus only** as the number, e.g. "Arcana +4" (Novice +2 / Adept +4 / Expert +6 / Master +8 / GM +10), with a small note that the attribute is added in play (it varies by use). Deliberately NOT attribute+mastery (that would imply a fixed attribute the rules do not define). Builder-only, small: `sheet()` already computes the tier for trades (`build_engine`/`builder_build` sheet block), so add the flat `MB[tier]` as the trade's bonus and render it in `shBuild`. Sits alongside the other feature-3 sheet polish.

**FR-16. Show DR on the Companion character page.** Add a Damage Reduction line to a character's Companion page, right under the `defDetail` line (which currently shows "PD: Heavy X · Brutal Y | AD: Heavy X · Brutal Y", template.html ~l.569). Decisions (Darryl 2026-07-16): **compact abbreviations**, e.g. "DR: PDR Half · MDR Half", listing only the types that apply (PDR/MDR/EDR); **omit the line entirely** for a character with no DR (so nothing shows until it has some). Two parts of the same FR, build in order:

- **Part A (static, engine-derived).** `companion-src/build.py` bakes the engine's derived `dr` into `party_derived[k]` (it currently bakes only the numeric stats), and template.html overwrites `CHARS[k].dr` from `PARTY_DERIVED` and renders the line. Runt shows "DR: PDR Half · MDR Half" now; the rest stay blank until **FR-2** populates their DR, so FR-16 pairs with FR-2. (DR shape from the engine is like `{"PDR":["half"],"MDR":["half"]}`.)
- **Part B (toggle-aware / buffed DR).** Make the DR line react to active toggles the way PD already does (`curPD()` / the "PD (buffed)" swap). Bonan's Rage toggle reads "half Phys/Elem dmg" = PDR Half + EDR Half while raging, but toggles currently only carry a `pd` delta. Give toggles an optional DR payload and add a `curDR()` that merges base gear DR with any active toggle DR - **"take the stronger", not additive** (DR does not stack: half + half is still half; a numeric vs "half" takes whichever reduces more). So Rage lights up "DR: PDR Half · EDR Half" while active and clears when it drops. Companion-only. **DONE 2026-07-17 (pushed on top of `394a8af` + Chrome-verified):** added the Rage DR payload + `curDR()` (with a `drRank` half-beats-numeric tie-break) + repointed the line to `fmtDR(curDR())`; `curDR()` is a passthrough when no DR toggle is active so FR-16A stays byte-identical. Verified in a fresh clone (catalog_verify 90/90, builder_verify PASS incl. two new FR-16B checks, node --check on the rebuilt Companion, plus a node harness for the merge). Companion-only.

**FR-17. Plan skill/trade picks can raise a Mastery Limit (cap+) + trades in plans.** Follow-up to FR-3 Slice 2, raised by Darryl on live-verify 2026-07-17. The Slice-2 plan skill picker offered a skill's next tier up ONLY within the normal level Mastery Limit (`_mastery_cap_idx`: Novice <L5, Adept L5-9, Expert L10). It did not expose the RAW "spend 1 Skill Point to raise the Mastery Limit of a Skill by 1" purchase (core-rules.md l.991-1005), which the flat-aggregate allocator already supports via `set_limit_raise` / the "cap+" checkbox / `limit_raise: skill_point_purchase`. So a builder-generated plan could not push a skill above the level cap the way Tanrielle's hand-authored L5 does. Darryl's two scoping calls (2026-07-18): **cap+ TOGGLE on the slot** (mirroring the allocator's checkbox), and **also add TRADES to plans** (beyond FR-17 as written; trades were not in plan levels at all).

**DONE 2026-07-18 - SHIPPED, PUSHED + LIVE-VERIFIED (origin head `33a9119 "FR-17"` on `96c6bb7`; Darryl confirmed a live screenshot: a planned L5 shows a "1 trade point" row with a trade picker beside the "2 skill points" skill picker, and both skill and trade slots carry a working cap+ checkbox that flips the picker into cap+ mode).** Builder-only, no ledger reshape (stays inside the Slice-2 carrier model), so catalog_verify stays 90/90 by construction and the Companion is unaffected. What shipped, all in `tools/builder_build.py` (the `API_PY` glue + the `TEMPLATE` JS) plus the harness:

- **Trades in plans.** `'trades':'trade'` joins `GRANT_CHILD_SLOTS`; `_gen_level_slots` emits a trade carrier `{slot:'trades', grants:{trades:M}}` (M = the class-spine `trade_points`) for PLAN levels, exactly like the skills carrier; a `trade` branch in `_options_for` + the point-buy branch in `_grant_children` materialise level-aware trade child pickers. New `PLAN_POINTBUY = ('skills','trades')` names the two point-buy carriers, and the running-state / options / running-caps / enforcement are all generalised over `kind` (`_plan_running_state`, `_plan_running_caps`, `_plan_options`, `_plan_catalog_names`).
- **cap+ toggle.** A `" (cap+)"` suffix on a granted value ("Acrobatics: Expert (cap+)") records a Mastery-Limit purchase: the pick may sit ONE tier above the level cap and costs 2 points (tier step + limit raise). A bare `CAPARM = '(cap+)'` sentinel = a slot ARMED for cap+ but not yet filled (so the picker offers the above-cap option before a skill is chosen). New `set_plan_capraise(did, on)` toggles it (arms/disarms an empty slot, or adds/removes the marker on a decided pick). Options in cap+ mode raise the offered ceiling by 1 and carry the marker; `_plan_running_caps` counts BOTH a flat-aggregate `limit_raise` (a carried purchase, so Tanrielle's Awareness Adept->Expert is legal without a NEW cap+) AND each cap+ pick in lower plan levels.
- **Points-based enforcement.** `builder_problems` now reports the skill/trade budgets by POINTS (a cap+ pick = 2), so a level is "N of M point(s) unspent" (under) or "over budget (M points, N spent)" (a cap+ overshoot at a 1-point level), replacing the old per-slot undecided flags. `catalog_problems` validates each decided skill/trade pick (known name, single step up, within `level cap + carried raises + own cap+`, distinct within the level). A cap+ purchase consumes budget as we go, so once a level's points are spent the trailing empty slot is dropped.
- **UI.** Each plan skill/trade slot renders a `cap+` checkbox (`data-plr`, wired to `set_plan_capraise`), mirroring the allocator's cap+ control; the carrier still renders as a fixed "N skill/trade points" header.

Verified in a fresh `/tmp` clone at head `96c6bb7`: **catalog_verify 90/90 zero deltas**, **builder_verify PASS** including a NEW section **(20)** (skills cap+ arm -> above-cap option -> pick -> marker stored -> 2-point cost -> engine/catalog legal -> disarm re-flags over-cap; trades carrier + trade child pickers + points-based trade enforcement), with sections **(13)/(18)/(19)** updated for the trades backbone, the points-based aggregate messaging, and the now-correct carried-cap behaviour; inline JS `node --check` OK; `builder.html` regenerated (2,459,409 bytes). **Pushed files:** `tools/builder_build.py`, `tools/builder_verify.py`, regenerated `builds/builder.html`, and this `BACKLOG.md` (this status flip rides the next commit). **Behaviour note (surfaced at live-verify):** cap+ ALWAYS charges the extra point, even when the pick is already within the level cap (mirrors the flat allocator's cap+ checkbox), so ticking cap+ on a 1-point trade budget reads "over budget (1 points, 2 spent)" - intended; cap+ earns its keep pushing a pick one tier ABOVE the cap (a held-Adept skill to Expert before L10). A follow-up refinement could hide/no-op cap+ when the step is already within the cap, if that ever bites in play.

---

## Features (2026-07-18 intake)

Builder and Companion items from Darryl's list. Grouped by surface for readability; IDs are flat.

### Builder

**FR-18. Replace the "Rung 3 - Step 5" top-left pill.** The build-stage pill is stale now the builder is functionally complete. Decide a replacement: candidate "v0.10.5" (the ruleset version this models), or a build stamp like the Companion's, or drop it entirely. **Decision (Darryl 2026-07-18): replace with "v0.10.5".** Cosmetic.

**FR-19. Refresh the "Decisions: Point-Buy, Option-Picker, Ancestry-Spend" subtitle.** Check whether that subtitle still describes the decision surface now that option-pickers are used almost everywhere; update or simplify to match. Cosmetic.

**FR-20. Reorder a level's decision pickers to match chargen flow.** Within a level the pickers appear in an order that may not follow how you actually build a character (e.g. Runt L1 currently reads spell schools, ancestries, pact boon, spells, maneuvers). Reorder to the natural chargen sequence. Needs a target ordering from Darryl (e.g. ancestry, then class/subclass, then resources), then it is a render-order change in the decisions loop. **DONE 2026-07-18 (Wave B pass 2, AWAITING PUSH).** Order = Darryl's call **attributes -> class/subclass -> ancestry -> resources**. Stable reorder in `_decisions()` via `_reorder_decisions` + the `FR20_CAT` rank map; grant-children stay glued under their parent (they inherit the parent block's rank + sequence). Pure display order, no id/engine/budget change. Harness section (21). See the Wave B pass 2 shipped note above.

**FR-21. Nested collapsibles for long level sections.** Long level groups (e.g. Runt L1) could carry collapsibles nested inside the level collapsible to tame their length. Open question: is nesting worth it, or is it clutter / overkill? Decide before building. **DONE 2026-07-18 (AWAITING PUSH).** Darryl's call: **category sub-headers, NOT nested collapsibles** (nesting adds a second click-layer to reach any pick; sub-headers give the scan benefit for free and pair with FR-36's colours). Implemented in the render loop: a level with **>=5 rows** (in practice only chargen L1) has its rows grouped under light per-category sub-headers (`.deccat`, one per category, coloured tick matching FR-36); short level-up levels (<5 rows) render flat as before. Rows are already category-sorted (FR-20), so each category is one contiguous run = one sub-header. Builder-only; harness section (25).

**FR-22. Remove or replace the "sources: N fetched, N baked" footer note.** The per-section "sources: 0 fetched, 22 baked" line is a dev-facing diagnostic. Remove it, or replace that spot with something useful (candidates: nothing, a level summary, a validation/legality status). **Decision (Darryl 2026-07-18): remove it.** Cosmetic.

**FR-23. Stamina Regen display.** Show each character's Stamina Regen trigger. Default = the character's own class trigger; with Martial Expansion (when the class has no Martial path of its own) the player may take another class's Martial path trigger OR the generic Spellcaster one; a class that starts with no Martial path just gets the Spellcaster trigger. Show it in BOTH the builder and the Companion. Needs rules grounding (which classes have a Martial path, the exact trigger wording, the Spellcaster Stamina Regen errata already recorded in `rules/_INDEX.md`) and a data source (likely catalog); scope before building. Builder + Companion (+ catalog).

**DONE 2026-07-18 (PUSHED + CHROME-VERIFIED, origin `3dd3cb9`).** Scope calls settled with Darryl: **data source = catalog-driven** (on-brand, door-open) and **Spellblade = the errata wording** (Darryl authoritative `_INDEX.md`, supersedes the classes.md l.2829 "Spell Attack" text). Grounding found: only 5 classes define a native Martial-path Stamina Regen in `classes.md` (Barbarian, Champion, Commander, Monk, Spellblade); Warlock/Druid have none and gain the Spellcaster trigger only on first Martial Path (`character-creation.md` l.741-744). For OUR six the Martial Expansion case touches only **Runt** (Warlock: Spellcaster rider + Monk via Martial Expansion = two triggers, 1 benefit/Round); **Scaletrix** (Druid, SP 0, never took a Martial Path) shows **None**; the other four show their native class trigger. Implementation: new curated **`builds/catalog/stamina_regen.yaml`** (class->text map + Spellcaster fallback); a shared **`build_engine.stamina_regen(ledger, regen_cat)`** helper derives the trigger list (catalog-agnostic engine: data is passed in), called by BOTH surfaces so they can never diverge; **`runt.yaml`** gained a structured `regen_source: Monk` on its Martial Expansion slot. Builder: `stamina_regen` added to the baked `CATALOG` + CATPATHS; `sheet()` returns the trigger list; a "Stamina Regen" row renders under Vitals (multi-trigger PCs show the 1-benefit/Round note). Companion: `build.py` bakes the trigger(s) into `PARTY_DERIVED`; a line renders under the SP tracker (hidden when SP 0 / no trigger). Verified in a fresh clone at `7676679`: **catalog_verify section 4** reconciles the catalog vs `classes.md`/`_INDEX.md`/`character-creation.md` and resolves all six ledgers to the expected labels (90/90 intact); **builder_verify section 27** confirms each PC's sheet triggers + the errata wording + the rendered row (PASS exit 0); Companion rebuild clean (`gm files: 0`, ends `</html>`, no image leak); regenerated `builds/builder.html` cp'd clone->mount **sha256 MATCH**; inline JS `node --check` OK (both artifacts); no em-dashes added. **Files:** `builds/catalog/stamina_regen.yaml` (new), `tools/build_engine.py`, `builds/runt.yaml`, `tools/builder_build.py`, `companion-src/build.py`, `companion-src/template.html`, `tools/catalog_verify.py`, `tools/builder_verify.py`, `builds/builder.html`, `builds/BACKLOG.md`. Suggested message: `FR-23 Stamina Regen display (catalog-driven; builder + Companion)`. **Judgment call (resolved 2026-07-18):** the per-PC `roundnote` prose duplicated the new structured line for tan/runt/xan; the duplicated trigger text was trimmed in the post-FR-23 cleanup batch (unique bits kept: Tan's "SP refills fully at combat end", Runt's Warlock Bargains + the casting-and-wrestling self-fuel note, Xan's Spellstrike/Blink Blade/Smite). See the cleanup-batch note above. Live-verify after push (Chrome): open the Companion for Runt and confirm the two-trigger line under SP + the 1-benefit/Round note; open Scaletrix and confirm no regen line; open the builder sheet for any PC and confirm the Stamina Regen row under Vitals.

**FR-36. Colour-coded left accent on decision rows by category.** Idea raised by Darryl 2026-07-18 at Wave B pass 2 live-verify: give each decision row a coloured left border keyed to its category (the FR-20 axis: attributes / class-subclass / ancestry / resources), so a long level list is easier to scan at a glance (his example: Runt L1, which is attributes + 3 spell schools + pact boon + 7 ancestry traits + 4 spells + 2 maneuvers). Cheap to build: reuse the existing `FR20_CAT` rank map in `builder_build.py` so colour == category with no new classification logic; it is a CSS class per category + the accent styling. **Design calls before building:** (1) the palette - keep it muted / not garish and colour-blind-safe, and ideally pair the colour with a non-colour cue (the L-badge is already there; a subtle per-category label could ride too) so meaning is not colour-alone; (2) how it interacts with the existing amber left accent - `.dec.newlvl { border-left:4px solid var(--warn) }`, applied whenever a row's note contains "Added in builder" (`isnew` in `rowHTML`), which currently marks builder-added / builder-touched rows (e.g. Runt's reconciled L2/L4 maneuvers). Either the category colour REPLACES that amber (moving "builder-touched" to a different cue), or the two coexist. Note the amber is now slightly vestigial on those reconciled-but-canonical maneuvers (BUG-16 removed their red x but left the accent), so this FR is a chance to rationalise left-accent semantics. Pairs thematically with **FR-21** (nested collapsibles) as the "tame long level lists" pair - worth deciding them together. Builder-only, cosmetic; ready once the palette + amber-interaction calls are made. **DONE 2026-07-18 (AWAITING PUSH).** Design calls settled with Darryl: **(1) palette** = colour-blind-safe four - attributes **blue #185FA5**, class/subclass **coral #C2410C**, ancestry **teal #1D9E75**, resources **amber #BA7517**; non-colour cue is the existing per-row uppercase slot label (SPELL / MANEUVER / ANCESTRY_TRAIT / SUBCLASS...), so meaning is never colour-alone. **Class is coral, not purple** - the first live-verify used purple #534AB7 which blurred against the adjacent blue attributes accent, so Darryl chose a warm coral for clear separation. **(2) amber interaction** = category colour **REPLACES** the old `.dec.newlvl` amber left border, which is **dropped entirely**. The first pass moved the "builder-touched" cue to the amber note text and surfaced it on editable rows too, but that dumped 13 dev-bookkeeping notes on Runt's pickers alone ("Added in builder", "ASK PHIL", catalog-model prose) - Darryl flagged it as clutter, so that was reverted: editable pickers no longer surface `t.note` (fixed/inferred rows still do, as before), and the builder-touched signal is simply gone (it was never player UI). Implementation: `_reorder_decisions` persists the category rank on each row as `d['cat']` (GC children inherit the parent block's cat, so a block is one colour); the JS `rowHTML` adds a `cat0..cat3` class (placed after `.edit`/`.plan` in the CSS so the coloured left border wins on those rows); the four `.dec.catN` rules carry the hexes. **Note: the builder is a light-only page** (single `:root`, no `prefers-color-scheme`), so the accents render on the fixed light background regardless of OS/app theme; if dark mode is ever added the accent hexes should become CSS variables. Builder-only; harness section (24).

### Companion

**FR-24. AP tracker relabel + left-to-right depletion.** The AP tracker is ambiguous (is a solid pip used or available?) and depletes right-to-left, against expectation. Change: label the left as "AP avail:" followed by the count of unused AP; deplete the pips left-to-right; "New turn" refills all 4; pips empty as AP is spent. Direction decided. **DONE 2026-07-18 (Wave B pass 1, PUSHED + CHROME-VERIFIED):** label reads "AP avail: N" (N = unused AP, gold); a pip is spent iff `i < 4-ap` (leftmost empties first); clicking a pip sets `ap = i<4-ap ? 4-i : 3-i` (fills-to or spends-through, toggle-consistent); `newRound()` unchanged (refills to 4).

**FR-25. Damage Calculator section (epic).** A new section between "Roll" and "Skills & Trades" that totals a hit's damage. Inputs: base weapon damage (1-3, ideally looked up from the character's weapon as a default), a hit-grade chooser (Normal / Heavy / Brutal / Brutal+ / Brutal++), a Crit yes/no toggle, and per-character option add-ons (Rage, Smite, Spellstrike, +1 martial enhancement, SP / MP spent, etc), with a running total. Optional auto-fill from the Roll section: grade from a filled-in "vs Def", Crit from a natural 20. Large: treat as an epic and design first (which add-ons apply per class and how each computes, and the Roll -> calculator auto-fill wiring).

**DESIGN (decided 2026-07-19, Darryl's three scope calls all = the recommended option). SHIPPED + PUSHED + CHROME-VERIFIED, origin `a349201` (v1 `44e6e9c` + polish `b17793f` + data corrections `a349201`; see the dated notes below). NOTE: the per-PC add-on details in the dated v1 note were superseded by the data-correction note (Smite split, Spellstrike, base fixes) - trust the corrections note + the catalog file.** Grounded in DC20's own "Adding Damage Order" sidebar (`rules/core-rules.md` l.2376): Base -> Heavy/Crit -> AP-spent -> Situational. The calculator IS that order, so it is not invented structure.

- **Scope call 1 = single-hit total (v1).** The section answers "what is THIS one hit worth" for every PC: base + hit-grade + crit + additive single-target riders. IN SCOPE for every PC incl. their single-target lines. OUT of v1: AoE / per-target / N-targets math (Bonan Whirlwind +1/target, Scaletrix Fireball/Breath area + nova rounds, Runt Chain), target Damage Reduction (calculator reports damage DEALT pre-DR; crit note says "bypasses DR"), and ruling-dependent engines (Scaletrix cheese-grater / Rolling Wild Fire, still waiting on Jesse per file 16). Those are a possible v2.
- **Scope call 2 = catalog-driven data.** New curated **`builds/catalog/damage_addons.yaml`** (class -> add-on list), baked into `PARTY_DERIVED` by `companion-src/build.py`, exactly the FR-23 `stamina_regen.yaml` precedent. A shared engine helper (`build_engine.damage_addons(ledger, cat)` or similar) resolves the per-PC list so builder + Companion can't diverge if the builder ever grows a calculator too.
- **Scope call 3 = one-tap pull button.** The Roll section already grades Hit/Heavy/Brutal/Beyond-Brutal from "vs Def" and flags nat-20 crits (`roll()` in template.html, `tier` var). A "pull from last roll" button copies grade + crit into the calculator on demand (NOT live auto-sync, which would overwrite manual what-ifs). Store the last roll's grade/crit in a small module var when `roll()` runs.

**The spine (5 rows):** (1) Base Damage stepper, default = the PC's primary weapon/spell base, editable (most PCs have both a weapon swing and a spell base). (2) Hit-grade chooser: Hit +0 / Heavy +1 / Brutal +2 / Beyond Brutal +3 (the "+1 per 5 over Defense" line). (3) Crit toggle: +2, auto-hit, bypasses DR (stacks on top of grade). (4) Per-character add-ons (below). (5) Running total, live.

**Two add-on widget types cover everything:** a **toggle** (fixed +N on/off) and a **capped stepper** (+N each, 0..cap).

**Per-PC add-on catalog content (L4, single-target additive only; source files 08-16):**
- **Common patterns:** Smite = +2 per SP, single target (bundles a free Damage enhancement, so 1 SP = +2), Tan+Xan. MP->damage = +2 per MP, cap = MSL = 2 at L4 / 3 at L5 (p61 rule, `combat.md` l.1807), all casters/hybrids. Imbue = +1 flat toggle, martial attacks only (not bolts). Powerful focus = +1 toggle (casters). Rage = +1 toggle (Bonan).
- **Tanrielle (Spellblade):** base greatsword 2 / Radiant Bolt 1. Smite +2/SP; Powerful +1 (toggle); MP->dmg +2/MP cap 2; Death's Toll +1 (toggle, vs Bloodied). (L6: Radiant imbue +1 toggle - add when she levels.)
- **Xanwyn (Spellblade):** base short sword 1 / Death Bolt. Smite +2/SP; Umbral imbue +1 (toggle); MP->dmg +2/MP cap 2. (Call of the Grave = accuracy, NOT damage, excluded.)
- **Runt (Warlock):** base Lightning Bolt 1 (staff swing minor). MP->dmg +2/MP cap 2; Lightning imbue +1 (toggle, martial attacks only, not bolts).
- **Minimus (Commander):** base crossbow. Thin own-damage line (his value is granting allies attacks, out of a single-hit calc): generic Damage +1 (toggle/AP); Battlefield Tactics +1 (toggle, flanked). Note the thinness in the UI.
- **Bonan (Barbarian):** base mace 2 (model un-raged; Rage as a toggle). Rage +1 (toggle); generic Damage +1 (toggle, single); Erupting Elements +1 Fire (toggle, raging + in aura). Whirlwind +1/target = AoE, EXCLUDED v1.
- **Scaletrix (Druid):** base Fire Bolt 1. MP->dmg +2/MP cap 2 (Focused single-target); Powerful +1 (toggle). Area/Breath/nova = AoE, EXCLUDED v1.

**Build plan (when green-lit):** NEW `builds/catalog/damage_addons.yaml`; shared helper in `tools/build_engine.py`; `companion-src/build.py` bakes resolved add-ons into `PARTY_DERIVED`; `companion-src/template.html` new collapsible card + calc JS (steppers/toggles + running total + "pull from last roll") + a module var capturing the last roll's grade/crit in `roll()`; harness coverage in `catalog_verify.py` (reconcile catalog vs the six ledgers/rules) + a Companion-side sanity check. Deploy auto-rebuilds on `companion-src/**`. No engine math change to existing checks (catalog_verify stays 90/90 by construction if the helper is additive).

**FR-26. Stackable conditions.** Strictly, some conditions stack (Bleeding, Stunned) but the character-sheet pills only toggle on/off. Would need a count / stack UI. Darryl: ignore for now. Parked.

**FR-27. Line dividers in Maneuvers (and other long lists).** The spell list already has dividers between entries; add the same to Maneuvers, and review whether any other long list benefits.

**FR-28. Dice roller reset button.** Add a reset control to the dice tab that clears Count / Drop / Mod (and any other fields) back to defaults.

**FR-29. Capture the Android back button in Search.** When viewing a rule opened from Search on Android, the hardware / browser Back button currently exits the Companion page entirely; make it behave like the in-app "<- Search" (pop back to the results first). History / popstate handling. Mobile. **DONE 2026-07-18 (Wave B pass 1, PUSHED + CHROME-VERIFIED):** `openDoc` now does `history.pushState` (once per doc session), and a `popstate` listener closes the doc when one is active (back to the results, or to the origin sheet on the ↩ Sheet path) instead of leaving the app; the on-screen ← Search / ↩ Sheet buttons call `history.back()` so the pushed entry stays in sync (no dangling history). Wrapped in try/catch for `file://`. Scope = the rule-doc view (the stated failure mode); a back press from the bare results list is left as-is.

**FR-30. Skills & Trades section collapsible.** Make the character sheet's Skills & Trades section collapsible like Maneuvers and Trades. Pairs with FR-31.

**FR-31. Default collapse state for Maneuvers & Trades.** Maneuvers & Trades appears expanded by default while other sheet sections are collapsed. Decide the intended default (collapsed is neater, but it is a high-use section) and apply it consistently across the sheet sections. **Decision (Darryl 2026-07-18): collapse all sheet sections by default (Maneuvers & Trades included); FR-30's new Skills & Trades section gets the same treatment.**

**FR-32. Rules > Spells cleanup.** The Rules browser's Spells area carries an odd PDF-scan subcategory that does not serve navigation, and all spells render as one very long page. Drop the artefact subcategory and split the spells page (per school, per spell, or dividers) so it is browsable. Approach needs a call. Touches the shared rules corpus (`tools/rules_corpus.py`) and possibly the `rules/spells.md` source.

**FR-33. Rules > Ancestries split.** The Ancestries subsection is meaningful, but all ancestries sit on one long page that is hard to scan. Split per ancestry, or add line dividers between them.

**FR-34. Rules > Class Talents merge.** The opposite problem: Character Creation > Class Talents is many pages of one or two paragraphs each. Combine into fewer, longer pages.

**FR-35. Rules > Tables reorg.** Most entries under Tables are class progression tables; consider moving each into its class page. The Armour Examples table could live with the Armour rules (and if so, why not shields / weapons / focuses too). Needs a call on how far to take it, and it may interact with FR-32/33/34 as one rules-browser structure pass.

---

## Chores

**CH-1. Sheet re-audit.** Re-check all six ledgers' languages / skills / trades against the paper sheets. Justified: a same-total swap (Xanwyn) and a granted-language mis-model (Runt) both slipped through undetected, so other silent faithfulness errors are plausible. Feeds BUG-7.

**DONE 2026-07-18.** All six L4 sheet PDFs in `sheets/` machine-read via their form fields (mastery checkboxes + name fields, no OCR guessing). Result: **five of six ledgers reconcile exactly** (Bonan, Runt, Scaletrix, Tanrielle, Xanwyn - every skill, knowledge trade, custom trade and language matches, including the earlier BUG-1/BUG-2/BUG-8 fixes). The one real gap: **minimus's skills were empty** (`allocation_confidence: unknown`, the old "ASK KRISTIAN" placeholder) - but his sheet has per-skill checkboxes after all: Awareness/Acrobatics/Influence/Insight/Survival all Novice, plus History + Nature (knowledge trades) Novice. Filled in `minimus.yaml`; the maths reconcile exactly (6 SP = 5 Novice skills + 1 SP -> 2 TP for the two knowledge trades, and 1 TP -> 2 LP for his Limited languages), so his long-standing "6 SPARE" advisory clears to balanced/balanced/balanced. `builder_verify` section (12)'s SPARE example (which used minimus) now runs as a synthetic (unset one skill in memory) + a new check that the minimus baseline is balanced; section (19)'s "empty aggregate" plan-skill checks were reframed against the real aggregate (add-new offers unheld skills at Novice, held Novice skills offer as one-step raises). The fill also exposed **BUG-11** (chained-conversion engine bug, fixed, see above). Flags for the record, no action: the Scaletrix sheet's Level field still says "3" (file is named L4; his ledger + expected block are L4 and verify clean) and names him "SCALECTRIX MCQUEEN" (ledger: Scaletrix D'Nandrews); Tan's paper sheet says "Elvish" (matches post-CH-2); Xanwyn's paper sheet spells "Elvan" (noted in the ledger comment, paper stays as-is).

**CH-2. Standardise Elven spelling.** "Elvan" (Xanwyn ledger + ancestry catalog key + L4 report + Companion), "Elven" (correct), "Elvish" (Tan). No hard data-matching issue today because language values allow free text; the only exact-match dependency is the ancestry catalog key, so any rename there must change catalog and ledger together. Cosmetic, low priority.

**DONE 2026-07-18 (Darryl's call, corrects this entry's premise).** The RAW language name is **"Elvish"**, not "Elven": core-rules.md l.789/831 lists Elvish (Typical Speakers: Elves), the curated `languages.yaml` Mortal list says Elvish, and catalog_verify checks that list against the rules - so Tan was right all along. "Elven" appears in the rules only as the ancestry ADJECTIVE ("Elven Traits", "Elven Will"). Standardised accordingly: language = Elvish (xanwyn.yaml "Elvan" -> "Elvish"; Companion sknote "Elvan & Fey (limited)" -> "Elvish & Fey"), ancestry adjective = Elven (xanwyn.yaml ancestry string "Elvan (Elf + Beast trait lists)" -> "Elven (...)"; Companion subtitle "Elvan Clandestine Alchemist" -> "Elven ..."; ancestries.yaml comment). "Elvan" no longer appears anywhere in the repo. Also checked: the feared "ancestry catalog key" dependency does not exist - the catalog keys are Elf/Beastborn/etc and "Elvan" was only a comment. Xanwyn's paper sheet still says Elvan; noted in the ledger comment.

**CH-3. Vestigial root index.html.** The repo tracks a root `index.html` the live site does not serve (the deploy Action rebuilds `dist/index.html` from the template). Harmless drift; `git rm --cached index.html` would drop it. (Kept 2026-07-16.)

**DONE 2026-07-18 (as an instruction).** The file is not on the OneDrive mount and `sync-to-repo.bat` never deletes, so the untrack has to happen in Darryl's laptop clone: run `git rm --cached index.html` there before committing this thread's changes (the deletion then rides the same commit). The file stays on disk locally (--cached) and the live site is unaffected (it serves dist/, rebuilt from the template).

---

## Parked / out of scope

- **Gear Tier A (done):** the engine already reads structured effect fields off free-text equipment entries (item `pd`/`ad`/`saves`, and `pdr`/`edr`/`mdr` for DR). This is how PD/AD/saves/DR reconcile today. No work needed; adding an essential item = add an entry with its effect fields.
- **Gear Tier C (out of scope):** full gear system, weapon attack-line derivation, attunement / magic-item slots, encumbrance, shopping. The "encode structure, not effects" line from ROADMAP.
- **Full-auto round-trip (v2):** players self-committing exports. Deliberately parked; one person commits for now.
- **Player-facing curated rules slice / separate container:** long horizon, the "last thing to build".
- **Community tool (rung 4):** dismissed on maintenance cost.

---

## Needs clarification

Initial triage complete (2026-07-16): all of Darryl's two lists plus the scattered scan items are deduped and logged. New items land here first, then move up once scoped.
